<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY rust-download-http "https://static.rust-lang.org/dist/rustc-&rust-version;-src.tar.xz">
  <!ENTITY rust-download-ftp  "">
  <!ENTITY rust-md5sum        "c206536bb0e11bbf2ebae91504096e34">
  <!ENTITY rust-size          "228 MB">
  <!ENTITY rust-buildsize     "10 GB (324 MB instalado); adicionar 11 GB se executar os testes">
  <!ENTITY rust-time          "10 UPC (incluindo tempo de transferência; adicionar 15 UPC para testes,
ambos usando paralelismo=8)">
]>

<sect1 id="rust" xreflabel="rustc-&rust-version;">
  <?dbhtml filename="rust.html"?>


  <title>Rustc-&rust-version;</title>

  <indexterm zone="rust">
    <primary sortas="a-rust">Rust</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introdução ao Rust</title>
    <para>
      A linguagem de programação <application>Rust</application> foi projetada
para ser uma linguagem segura, simultânea e prática.
    </para>

    <para>
      Como com muitas outras linguagens de programação, rustc (o compilador da
rust) precisa de um binário a partir do qual autoaperfeiçoar-se. Ele baixará
um binário de estágio 0 no início da construção, de modo que você precisa
ter uma conexão com a Internet para construir esse pacote.
    </para>

    <note>
      <para>
        Embora o BLFS normalmente instale em <filename
class="directory">/usr</filename>, quando você atualizar posteriormente para
uma versão mais recente do <application>rust</application>, as bibliotecas
antigas em <filename class="directory">/usr/lib/rustlib</filename>
permanecerão, com vários resumos nos nomes delas, mas não serão usáveis e
desperdiçarão espaço. Os(As) editores(as) recomendam colocar os arquivos no
diretório <filename class="directory">/opt</filename>. Em particular, se
você tiver que reconstruir esse pacote com uma configuração modificada (por
exemplo, usando o LLVM enviado depois de construir com LLVM compartilhado,
talvez para compilar caixas para arquiteturas as quais a construção do LLVM
do BLFS não suporta), é possível para a instalação deixar um programa
quebrado <command>cargo</command>. Se isso ocorrer, você deveria ou remover
a instalação existente primeiro ou usar um prefixo diferente, como
/opt/rustc-&rust-version;-build2.
      </para>

      <para>
        Se preferir, você pode, certamente, mudar o prefixo para &quot;<filename
class="directory">/usr</filename>&quot;.
      </para>
    </note>

    <para>
      O atual sistema de construção do <application>rustbuild</application> usará
todos os processadores, embora não escale bem e frequentemente recorra a
usar somente um núcleo enquanto espera que uma biblioteca compile. No
entanto, ele pode ser limitado principalmente a um número especificado de
processadores por uma combinação da adição da chave <literal>--jobs
&lt;N&gt;</literal> (por exemplo, '--jobs 4' para limitar a 4 processadores)
em cada invocação de <command>./x.py</command> <emphasis>e</emphasis> de
usar-se a variável de ambiente <envar>CARGO_BUILD_JOBS=&lt;N&gt;</envar>. No
momento, isso não é eficaz quando alguns dos testes do rustc forem
executados.
    </para>

    <para>
      A versão atual da caixa num_cpus da rust agora reconhece que cgroups pode
ser usado para restringir quais processadores ele está autorizado a usar. Se
tua máquina carecer de DRAM (normalmente, menos de 2 GB de DRAM por núcleo),
isso pode ser uma alternativa para colocar CPUs offline. Leia-se <xref
linkend='build-in-cgroup'/> para informações acerca de como usar um cgroup.
    </para>

    <para>
      No momento, <application>Rust</application> não oferece nenhuma garantia de
uma ABI estável.
    </para>

    <note>
      <para>
        Rustc padroniza para construir para TODAS as arquiteturas suportadas, usando
uma cópia enviada do LLVM. No BLFS, a construção é somente para a
arquitetura X86. Se você vai desenvolver caixas rust, essa construção
possivelmente não seja suficiente para os teus propósitos.
      </para>
      <para>
        Os tempos de construção dessa versão, quando repetidos na mesma máquina,
frequentemente são razoavelmente consistentes, mas como com todas as
compilações usando-se <command>rustc</command>, podem existir alguns valores
discrepantes muito lentos.
      </para>
    </note>

    &lfs130_checked;

    <bridgehead renderas="sect3">Informação do Pacote</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Transferência (HTTP): <ulink url="&rust-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Transferência (FTP): <ulink url="&rust-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Soma de verificação MD5 da transferência: &rust-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Tamanho da transferência: &rust-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Espaço em disco estimado exigido: &rust-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Tempo de construção estimado: &rust-time;
        </para>
      </listitem>
    </itemizedlist>

    
    
    <!--  keep this here for reuse in case we need a patch -->
<!-- deliberately skipped turning version and md5 into entities:
         it's unlikely we'd still need this in the next rustc release.  -->
<bridgehead renderas="sect3">Transferências Adicionais</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Ligações Rust do OpenSSL atualizadas para suportar OpenSSL 4.: <ulink
url="https://github.com/lfs-book/rust-openssl/archive/v0.10.78/rust-openssl-v0.10.78.tar.gz"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Soma de verificação MD5 da transferência: 8bfcc36cba65c98380f7ac1ec07efdb4
        </para>
      </listitem>
    </itemizedlist>

    &github-lfs-fork;

    <bridgehead renderas="sect3">Dependências do Rust</bridgehead>

    <bridgehead renderas="sect4">Exigidas</bridgehead>
    <para role="required">
      <!-- Required for downloading stage 0 binaries.
           Otherwise it's recommended (if not installed, a vendored copy
           will be built). -->
<xref linkend="cmake"/> e <xref linkend="curl"/>
    </para>

    &build-use-internet;

    <bridgehead renderas="sect4">Recomendadas</bridgehead>
    <para role="recommended">
      <xref linkend="libssh2"/> e <xref linkend="llvm"/> (construído com -D
LLVM_LINK_LLVM_DYLIB=ON de modo que rust consiga lincar-se à versão
instalada do sistema do LLVM em vez de construir versão dele enviada)
    </para>

    <note>
      <para>
        Se uma dependência recomendada não estiver instalada, você precisará
modificar as instruções para construir e usar uma cópia enviada no tarball
do fonte do Rustc. Veja-se a seção Explicações dos Comandos para mais
detalhes.
      </para>
    </note>

    <bridgehead renderas="sect4">Opcionais</bridgehead>
    <para role="optional">
      <xref linkend="gdb"/> (usado pela suíte de teste se estiver presente), <xref
linkend="git"/> (exigido pela suíte de teste), <ulink
url="https://github.com/bytecodealliance/wasmtime/tree/main/cranelift">cranelift</ulink>,
<ulink url="https://jemalloc.net/">jemalloc</ulink>, libgccjit (leiam-se as
Explicações do Comando em <xref role="nodep" linkend="gcc"/>) e <ulink
url='https://libgit2.org/'>libgit2</ulink>
    </para>

    <para condition="html" role="usernotes">
      Observações de Editor(a): <ulink url="&blfs-wiki;/rust"/>
    </para>
  </sect2>

  <sect2 role="installation">
    <title>Instalação do Rust</title>

    <para>
      Para instalar no diretório &quot;<filename
class="directory">/opt</filename>&quot;, remova qualquer link simbólico
&quot;<filename>/opt/rustc</filename>&quot; existente e crie um novo
diretório (ou seja, com um nome diferente se tentar uma construção
modificada da mesma versão). Como o(a) usuário(a) &quot;<systemitem
class="username">root</systemitem>&quot;:
    </para>

<screen role="root"><userinput>mkdir -pv /opt/rustc-&rust-version;     &amp;&amp;
ln -svfn rustc-&rust-version; /opt/rustc</userinput></screen>

    <note>
      <para>
        Se múltiplas versões do &quot;<application>Rust</application>&quot;
estiverem instaladas em &quot;<filename
class="directory">/opt</filename>&quot;, [então] a mudança para outra versão
exige somente mudar o link simbólico
&quot;<filename>/opt/rustc</filename>&quot; e então executar
&quot;<command>ldconfig</command>&quot;.
      </para>
    </note>

    <para>
      Crie um arquivo <filename>bootstrap.toml</filename> adequado que configurará
a construção.
    </para>

<screen><userinput>cat &lt;&lt; EOF &gt; bootstrap.toml
<literal># See bootstrap.toml.example for more possible options,
# and see src/bootstrap/defaults/bootstrap.dist.toml for a few options
# automatically set when building from a release tarball
# (unfortunately, we have to override many of them).

# Tell x.py that the editors have reviewed the content of this file
# and updated it to follow the major changes of the building system,
# so x.py will not warn users to review that information.
change-id = 148671

[llvm]
# When using the system installed copy of LLVM, prefer the shared libraries
link-shared = true

# If building the shipped LLVM source, only enable the x86 target
# instead of all the targets supported by LLVM.
targets = "X86"

[build]
description = "for BLFS &version;"

# Omit the documentation to save time and space (the default is to build them).
docs = false

# Only install these extended tools. Cargo, clippy, rustdoc, and rustfmt
# are installed by a default rustup installation, and rust-src is needed
# to build the Rust code in Linux kernel (in case you need such a kernel
# feature).
tools = ["cargo", "clippy", "rustdoc", "rustfmt", "src"]

[install]
prefix = "/opt/rustc-&rust-version;"
docdir = "share/doc/rustc-&rust-version;"

[rust]
channel = "stable"

# Enable the same optimizations as the official upstream build.
lto = "thin"
codegen-units = 1

# Don't build llvm-bitcode-linker which is only useful for the NVPTX
# backend that we don't enable.
llvm-bitcode-linker = false

[target.x86_64-unknown-linux-gnu]
llvm-config = "/usr/bin/llvm-config"

[target.i686-unknown-linux-gnu]
llvm-config = "/usr/bin/llvm-config"</literal>
EOF</userinput></screen>

    <para>
      Use uma versão mais recente da ligação Rust do OpenSSL para permitir
construir esse pacote com OpenSSL 4:
    </para>

<!-- retain here in case we need a patch
    <para>

      If running the test suite, apply a patch to make it compatible with
      GCC-15 or newer:
    </para>

<screen><userinput remap="pre">patch -Np1 -i ../rustc-&rust-version;-testsuite_fix-1.patch</userinput></screen>
-->
<screen><userinput>tar xf ../rust-openssl-v0.10.78.tar.gz &amp;&amp;

cat &gt;&gt; src/tools/cargo/Cargo.toml &lt;&lt; EOF &amp;&amp;
<literal>[patch.crates-io]
openssl = { path = "../../../rust-openssl-0.10.78/openssl" }
openssl-sys = { path = "../../../rust-openssl-0.10.78/openssl-sys" }
</literal>EOF

sed -ri src/tools/cargo/Cargo.lock \
    -e '/name = "openssl-sys"/,/^$/{/source|checksum/d;s/0.9.111/0.9.114/}' \
    -e '/name = "openssl"/,/^$/{/source|checksum/d;s/0.10.75/0.10.78/}'     \
    -e '/name = "openssl-macros"/,/^$/{/source|checksum/d}'</userinput></screen>


    <para>
      Compile &quot;<application>Rust</application>&quot; executando os seguintes
comandos:
    </para>


<screen><!-- fixed in 1.58.0, retain as a reminder that fixed crates can be used
<screen>
<userinput>sed -i -e '/^curl /s/0.4.38/0.4.40/' \
       -e '/^curl-sys /s/0.4.48/0.4.50/' \
       src/tools/cargo/Cargo.toml &amp;&amp; -->
<userinput>export LIBSSH2_SYS_USE_PKG_CONFIG=1
export LIBSQLITE3_SYS_USE_PKG_CONFIG=1
./x.py build</userinput></screen>

    <note>
      <para>
        A suíte de teste gerará algumas mensagens no <phrase
revision="sysv">registro do sistema</phrase> <phrase
revision="systemd">diário do systemd</phrase> para interceptações sobre
códigos inválidos de operação e para falhas de segmentação. Por si só, elas
não são motivo de preocupação, e são apenas uma maneira para um teste ser
terminado.
      </para>
    </note>

    <para>
      Para executar os testes, emita:
    </para>

<screen remap="test"><userinput>./x.py test --verbose --no-fail-fast | tee rustc-testlog</userinput></screen>

    <para>
      <!-- https://github.com/rust-lang/rust/issues/154392 -->
<!-- https://github.com/rust-lang/rust/issues/155606 -->
Nos testes do <filename class='directory'>rustdoc-ui</filename>, o teste
chamado <filename>track-diagnostics.rs</filename> é conhecido por falhar
porque ele codifica rigidamente caminhos inexistentes no LFS, e o teste
chamado <filename>ice-bug-report-url.rs</filename> é conhecido por falhar
por motivo desconhecido. Nos testes do <filename
class='directory'>run-make</filename>, o teste chamado
<filename>remap-path-prefix-std</filename> é conhecido por falhar porque ele
não funciona com LTO habilitado; o teste chamado
<filename>rustdoc-target-modifiers</filename> é conhecido por falhar porque
nós não habilitamos o alvo AArch64 no <xref linkend='llvm'/>; e o teste
chamado <filename>short-ice</filename> é conhecido por falhar por motivo
desconhecido.
    </para>

    <para>
      Como com todas as grandes suítes de teste, alguns testes podem falhar em
algumas máquinas - se o número de falhas adicionais for baixo, verifique se
existem 'failures:' no registro e revise as linhas acima, especialmente as
linhas 'stderr:'. Qualquer menção a um SIGSEGV ou ao sinal onze (11) em um
teste falho é motivo de preocupação.
    </para>

    <para>
      Se você obtiver algum <emphasis>outro</emphasis> teste falho que informe um
número de problema, então você deveria procurar por esse problema. Por
exemplo, quando rustc &gt;= 1.41.1 era construído com uma versão do LLVM do
sistema anterior à 10.0, o teste para o problema 69225 falhava <ulink
url="https://github.com/rust-lang/rust/issues/69225"/> e isso deveria ser
considerado como uma falha crítica (eles(as) lançaram 1.41.1 por causa
disso). A maioria das outras falhas não será crítica.
    </para>

    <para>
      Portanto, você deveria determinar o número de falhas. O número de testes que
passaram e falharam pode ser encontrado executando-se:
    </para>


<screen remap="test"><!-- split into two lines for narrower screen windows -->
<userinput>grep '^test result:' rustc-testlog |
 awk '{sum1 += $4; sum2 += $6} END { print sum1 " passed; " sum2 " failed" }'</userinput></screen>

    <para>
      Os outros campos disponíveis são &quot;$8&quot; para aqueles que foram
ignorados (ou seja, pulados), &quot;$10&quot; para 'medido' e
&quot;$12&quot; para 'filtrado', mas ambos os dois últimos provavelmente
sejam zero.
    </para>

    <para>
      Agora, como o(a) usuário(a) &quot;&root;&quot;, instale o pacote:
    </para>

    <note>
      <para>
        Se <command>sudo</command> ou <command>su</command> for invocado para
comutar para o(a) usuário(a) &root;, certifique-se de que
<envar>LIBSSH2_SYS_USE_PKG_CONFIG</envar> e
<envar>LIBSQLITE3_SYS_USE_PKG_CONFIG</envar> sejam passados corretamente ou
o comando a seguir pode reconstruir <command>cargo</command> com cópias
enviadas da libssh2 e do sqlite. Para <command>sudo</command>, use a opção
<option>--preserve-env=LIB{SSH2,SQLITE3}_SYS_USE_PKG_CONFIG</option>. Para
<command>su</command>, <emphasis>não</emphasis> use as opções
<option>-</option> ou <option>--login</option>.
      </para>
    </note>

<screen role='root'><userinput>./x.py install</userinput></screen>

    <para>
      Ainda como o(a) usuário(a) &root;, corrija a instalação da documentação,
linque simbolicamente um arquivo de completação do
<application>Zsh</application> para o local correto, e mova um arquivo de
completação do <application>Bash</application> para o local recomendado
pelos(as) mantenedores(as) da completação do
<application>Bash</application>:
    </para>

    <screen role='root'><userinput>rm -fv /opt/rustc-&rust-version;/share/doc/rustc-&rust-version;/*.old   &amp;&amp;
install -vm644 README.md                                \
               /opt/rustc-&rust-version;/share/doc/rustc-&rust-version; &amp;&amp;

install -vdm755 /usr/share/zsh/site-functions      &amp;&amp;
ln -sfv /opt/rustc/share/zsh/site-functions/_cargo \
        /usr/share/zsh/site-functions

mv -v /etc/bash_completion.d/cargo /usr/share/bash-completion/completions</userinput></screen>

    <para>
      Finalmente, desconfigure as variáveis de ambiente exportadas:
    </para>

<screen><userinput>unset LIB{SSH2,SQLITE3}_SYS_USE_PKG_CONFIG</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Explicações do Comando</title>

    <para>
      <command>ln -svfn rustc-&rust-version; /opt/rustc</command>: se esse não for
o primeiro uso do link simbólico <filename
class="directory">/opt/rustc</filename>, sobrescreva-o por força e use o
sinalizador '-n' para evitar obter resultados confusos oriundos, por
exemplo, de <command>ls -l</command>.
    </para>

    <para>
      <literal>tools = ["cargo", "clippy", "rustdoc", "rustfmt"]</literal>:
construa somente as ferramentas originárias do perfil 'padrão', a partir do
comando binário <command>rustup</command>, as quais são recomendadas para a
maioria dos(as) usuários(as). As outras ferramentas são improváveis que
sejam úteis, a menos que se use analisadores (antigos) de código ou se edite
a biblioteca padrão.
    </para>

    <para>
      <literal>channel = "stable"</literal>: isso garante que somente recursos
estáveis sejam usados. O padrão no <filename>bootstrap.toml</filename> é o
de usar recursos de desenvolvimento, os quais não são apropriados para uma
versão lançada da rust.
    </para>

    
    <!-- comment while using shipped LLVM -->
<para>
      <literal>[target.x86_64-unknown-linux-gnu]</literal>: a sintaxe do
<filename>bootstrap.toml</filename> exige uma entrada
<literal>llvm-config</literal> para cada alvo para o qual o LLVM do sistema
esteja para ser usado. Mude o alvo para
<literal>[target.i686-unknown-linux-gnu]</literal> se você estiver
construindo em x86 de 32 bits. Esta seção inteira deveria ser omitida se
você desejar construir contra o LLVM enviado, porém a construção resultante
será maior e demorará mais.
    </para>



    
    <!--<para>

      <command>sed -i -e '/^curl /s/0.4.38/0.4.40/' ... </command>: two crates
      normally downloaded for this release do not correctly initialise
      <application>curl</application> if using
      <application>openssl-3.0.0</application>. Upstream has fixed that for a
      future release, this sed causes the fixed versions to be used.
    </para>-->
<!-- https://github.com/alexcrichton/ssh2-rs/issues/173 -->
<para>
      <command>export LIBSSH2_SYS_USE_PKG_CONFIG=1</command>: Permite
<command>cargo</command> se lincar à cópia instalada do sistema da
libssh2. Omita esse comando se você não tiver instalado <xref
linkend='libssh2'/>.
    </para>

    
    <!-- src/tools/cargo/Cargo.toml is specifying "bundled" as the
         upstream is suggesting to avoid issues building for Windoge.
         We don't care about Windoge so override this if we have
         system sqlite3.  -->
<para>
      <command>export LIBSQLITE3_SYS_USE_PKG_CONFIG=1</command>: Permite
<command>cargo</command> se lincar à cópia instalada do sistema do sqlite.
    </para>



    <!--<para>

      <command>export RUSTFLAGS="$RUSTFLAGS -C link-args=-lffi"</command>
:
      This adds a link to libffi to any RUSTFLAGS you may already be passing
      to the build. On some systems, linking fails to include libffi unless
      this is used. The reason why this is needed is not clear.
      2023-01-14 : assumed to be no longer needed, but it is some years
      since one person reported needing this, keep it commented for the moment.
    </para>-->
<para>
      <parameter>--verbose</parameter>: essa chave às vezes pode fornecer mais
informações relativas a um teste que falhe.
    </para>

    <para>
      <parameter>--no-fail-fast</parameter>: essa chave garante que a suíte de
teste não parará no primeiro erro.
    </para>

  </sect2>

  <sect2 role="configuration">
    <title>Configurando o Rust</title>

    <sect3 id="rustc-config">
      <title>Informação de Configuração</title>

      <para>
        Se você instalou &quot;<application>rustc</application>&quot; em
&quot;<filename class="directory">/opt</filename>&quot;, [então] você
precisa atualizar os seguintes arquivos de configuração, de forma que
&quot;<application>rustc</application>&quot; seja encontrado corretamente
por outros pacotes e processos do sistema.
      </para>

      <para>
        Como o(a) usuário(a) &quot;<systemitem
class="username">root</systemitem>&quot;, crie o arquivo
&quot;<filename>/etc/profile.d/rustc.sh</filename>&quot;:
      </para>

<screen role="root"><userinput>cat &gt; /etc/profile.d/rustc.sh &lt;&lt; "EOF"
<literal># Begin /etc/profile.d/rustc.sh

pathprepend /opt/rustc/bin           PATH

# End /etc/profile.d/rustc.sh</literal>
EOF</userinput></screen>

      <para>
        Imediatamente depois da instalação, atualize o PATH atual para teu shell
atual como um(a) usuário(a) normal:
      </para>

<screen><userinput>source /etc/profile.d/rustc.sh</userinput></screen>

    </sect3>
  </sect2>


  <sect2 role="content">
    <title>Conteúdo</title>

    <segmentedlist>
      <segtitle>Aplicativos Instalados</segtitle>
      <segtitle>Bibliotecas Instaladas</segtitle>
      <segtitle>Diretórios Instalados</segtitle>

      <seglistitem>
        <seg>
          cargo-clippy, cargo-fmt, cargo, clippy-driver, rust-gdb, rust-gdbgui,
rust-lldb, rustc, rustdoc e rustfmt
        </seg>
        <seg>
          librustc_driver-&lt;16-byte-hash&gt;.so e libstd-&lt;16-byte-hash&gt;.so
        </seg>
        <seg>
          ~/.cargo, /opt/rustc, link simbólico para /opt/rustc-&rust-version;
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descrições Curtas</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="cargo-clippy">
        <term><command>cargo-clippy</command></term>
        <listitem>
          <para>
            realiza verificações lint sobre um pacote da cargo
          </para>
          <indexterm zone="rust cargo-clippy">
            <primary sortas="b-cargo-clippy">cargo-clippy</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="cargo-fmt">
        <term><command>cargo-fmt</command></term>
        <listitem>
          <para>
            formata todos os arquivos &quot;bin&quot; e &quot;lib&quot; da caixa atual
usando &quot;Rustfmt&quot;
          </para>
          <indexterm zone="rust cargo-fmt">
            <primary sortas="b-cargo-fmt">cargo-fmt</primary>
          </indexterm>
        </listitem>
      </varlistentry>



      <!--  <varlistentry id="cargo-miri">

        <term><command>cargo-miri</command></term>
        <listitem>
          <para>
            is for use by Miri to interpret bin crates and tests. It is
            not installed by default.
          </para>
          <indexterm zone="rust cargo-miri">
            <primary sortas="b-cargo-miri">cargo-miri</primary>
          </indexterm>
        </listitem>
      </varlistentry>-->
<varlistentry id="cargo">
        <term><command>cargo</command></term>
        <listitem>
          <para>
            é o Gerenciador de Pacote para &quot;Rust&quot;
          </para>
          <indexterm zone="rust cargo">
            <primary sortas="b-cargo">cargo</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="clippy-driver">
        <term><command>clippy-driver</command></term>
        <listitem>
          <para>
            realiza verificações lint sobre código da Rust
          </para>
          <indexterm zone="rust clippy-driver">
            <primary sortas="b-clippy-driver">clippy-driver</primary>
          </indexterm>
        </listitem>
      </varlistentry>



      <!--  <varlistentry id="miri">

        <term><command>miri</command></term>
        <listitem>
          <para>
            is an interpreter for Rust's mid-level intermediate representation
            (MIR). It is not installed by default.
          </para>
          <indexterm zone="rust miri">
            <primary sortas="b-miri">miri</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rls">
        <term><command>rls</command></term>
        <listitem>
          <para>
            is the Rust Language Server. This can run in the background to
            provide IDEs, editors, and other tools with information about Rust
            programs
          </para>
          <indexterm zone="rust rls">
            <primary sortas="b-rls">rls</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rust-analyzer">
        <term><command>rust-analyzer</command></term>
        <listitem>
          <para>
            is an implementation of Language Server Protocol for the Rust
            programming language.
          </para>
          <indexterm zone="rust rust-analyzer">
            <primary sortas="b-rust-analyzer">rust-analyzer</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rust-demangler">
        <term><command>rust-demangler</command></term>
        <listitem>
          <para>
            converts a list of Rust mangled symbols into a
            corresponding list of demangled symbols
          </para>
          <indexterm zone="rust rust-demangler">
            <primary sortas="b-rust-demangler">rust-demangler</primary>
          </indexterm>
        </listitem>
      </varlistentry> -->
<varlistentry id="rust-gdb">
        <term><command>rust-gdb</command></term>
        <listitem>
          <para>
            é um conjunto de comandos sequenciados envolucradores para gdb, os quais
extraem os módulos de impressão bonita do Python instalados em <filename
class="directory">/opt/rustc-&rust-version;/lib/rustlib/etc</filename>
          </para>
          <indexterm zone="rust rust-gdb">
            <primary sortas="b-rust-gdb">rust-gdb</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rust-gdbgui">
        <term><command>rust-gdbgui</command></term>
        <listitem>
          <para>
            é um &quot;script&quot; envólucro para uma estrutura gráfica de interação
direta com o(a) usuário(a) para &quot;gdb&quot; que executa em um navegador
          </para>
          <indexterm zone="rust rust-gdbgui">
            <primary sortas="b-rust-gdbgui">rust-gdbgui</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rust-lldb">
        <term><command>rust-lldb</command></term>
        <listitem>
          <para>
            é um conjunto de comandos sequenciados envolucradores para LLDB (o depurador
LLVM) os quais extraem os módulos de impressão bonita do Python
          </para>
          <indexterm zone="rust rust-lldb">
            <primary sortas="b-rust-lldb">rust-lldb</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rustc">
        <term><command>rustc</command></term>
        <listitem>
          <para>
            é o compilador &quot;rust&quot;
          </para>
          <indexterm zone="rust rustc">
            <primary sortas="b-rustc">rustc</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rustdoc">
        <term><command>rustdoc</command></term>
        <listitem>
          <para>
            gera documentação a partir do código fonte &quot;rust&quot;
          </para>
          <indexterm zone="rust rustdoc">
            <primary sortas="b-rustdoc">rustdoc</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rustfmt">
        <term><command>rustfmt</command></term>
        <listitem>
          <para>
            formata código &quot;rust&quot;
          </para>
          <indexterm zone="rust rustfmt">
            <primary sortas="b-rustfmt">rustfmt</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libstd">
        <term><filename
class="libraryfile">libstd-&lt;resumo_de_16_bytes&gt;.so</filename></term>
        <listitem>
          <para>
            é a biblioteca padrão do &quot;Rust&quot;, a fundação do logiciário portável
&quot;Rust&quot;
          </para>
          <indexterm zone="rust libstd">
            <primary sortas="c-libstd">libstd-&lt;resumo_de_16_bytes&gt;.so</primary>
          </indexterm>
        </listitem>
      </varlistentry>
    </variablelist>
  </sect2>

</sect1>
