<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY kea-download-http "https://downloads.isc.org/isc/kea/&kea-dhcp-version;/kea-&kea-dhcp-version;.tar.gz">
  <!ENTITY kea-download-ftp  "">
  <!ENTITY kea-md5sum        "ad23d02b3ce0475eb077da05b0ae2429">
  <!ENTITY kea-size          "10 MB">
  <!ENTITY kea-buildsize     "1.5 GB (332 MB installed; add 4 GB for tests)">
  <!ENTITY kea-time          "4.1 SBU (with parallelism=4; add 12 SBU for tests)">
  <!ENTITY kea-arm-vers      "&kea-dhcp-version;">
]>

<sect1 id="kea" xreflabel="Servidor DHCP Kea-&kea-dhcp-version;">
  <?dbhtml filename="kea.html"?>

  <title>Servidor DHCP Kea &kea-dhcp-version;</title>

  <indexterm zone="kea">
    <primary sortas="a-KEA">Servidor DHCP Kea</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introdução ao Servidor DHCP Kea do ISC</title>

    <para>
      O pacote <application>ISC Kea</application> contém os aplicativos de
servidor para DHCP. É o sucessor do antigo servidor DHCP do ISC, que está em
fim de vida desde dezembro de 2022.
    </para>

    &lfs121_checked;

    <bridgehead renderas="sect3">Informação do Pacote</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Transferência (HTTP): <ulink url="&kea-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Transferência (FTP): <ulink url="&kea-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Soma de verificação MD5 da transferência: &kea-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Tamanho da transferência: &kea-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Espaço em disco estimado exigido: &kea-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Tempo de construção estimado: &kea-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependências do Kea</bridgehead>

    <bridgehead renderas="sect4">Exigidas</bridgehead>
    <para role="required">
      <xref linkend="boost"/> e <xref linkend="log4cplus"/>
    </para>

    <bridgehead renderas="sect4">Opcionais</bridgehead>
    <para role="optional">
      <xref linkend="mitkrb"/>, <xref linkend="valgrind"/>; for documentation:
<xref linkend="doxygen"/>, <xref linkend="graphviz"/>, and <xref
linkend="sphinx_rtd_theme"/>; for tests: <ulink
url="https://google.github.io/googletest/">GoogleTest</ulink>
    </para>

    <bridgehead renderas="sect4">Estruturas opcionais de retaguarda de base de dados</bridgehead>
    <para role="optional">
      <xref linkend="mariadb"/> ou <ulink
url="https://www.mysql.com/">MySQL</ulink> e <xref linkend="postgresql"/>
    </para>

  <!--
    <para condition="html" role="usernotes">
Editor Notes:
      <ulink url="&blfs-wiki;/kea"/>
    </para>
-->
</sect2>

  <sect2 role="kernel" id="kea-dhcp-kernel">
    <title>Configuração do Núcleo</title>

    <para>
      Você precisa ter suporte a Packet Socket. Suporte a IPv6 é opcional.
    </para>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
      href="kea-kernel.xml"/>

    <indexterm zone="kea kea-dhcp-kernel">
      <primary sortas="d-KEA">Kea</primary>
    </indexterm>

  </sect2>

  <sect2 role="installation">
    <title>Instalação do Servidor DHCP Kea do ISC</title>

    <para>
      First fix detection of Python-3.12 by the build system:
    </para>

<screen><userinput>sed -e 's/:3/:4/' \
    -i configure</userinput></screen>

    <para>
      Remove one installation step that uses an obsolete python module:
    </para>

<screen><userinput>sed -e '/dlist="/d' \
    -i src/bin/shell/Makefile.in</userinput></screen>

    <para>
      Instale o <application>Servidor DHCP Kea do ISC</application> executando os
seguintes comandos:
    </para>

<screen><userinput>./configure --prefix=/usr        \
            --sysconfdir=/etc    \
            --localstatedir=/var \
            --enable-shell       \
            --with-openssl       \
            --disable-static     \
            --docdir=/usr/share/doc/kea-&kea-dhcp-version; &amp;&amp;
make</userinput></screen>

    <para>
      To test the results, you must have installed GoogleTest and kept its
source. You should also have passed
<option>--with-gtest-source=/path/to/googletest/sourcedir</option> to
<command>configure</command> above. Run the tests with <command>make
check</command>. Three tests in the TLSTest suite are known to fail.
    </para>

    <para>
      Para instalar a suíte <application>Servidor DHCP Kea do ISC</application>,
emita os seguintes comandos como o(a) usuário(a) <systemitem
class="username">root</systemitem>:
    </para>

<screen role="root"><userinput>make -j1 install</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Explicações do Comando</title>

    <para>
      <parameter>--enable-shell</parameter>: Allows building
<command>kea-shell</command>, a command line interface for the control
agent.
    </para>

    <para>
      <parameter>--with-openssl</parameter>: Allows using OpenSSL for
communicating with the control-agent and for DNS updates.
    </para>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
      href="../../xincludes/static-libraries.xml"/>

    <para>
      <option>--with-pgsql</option> or <option>--with-mysql</option>:
<application>ISC Kea</application> can store the leases on a database. This
might be useful in large environments running a cluster of DHCP
servers. Using the <emphasis>memfile</emphasis> backend (which is a CSV file
stored locally) is possible anyhow.
    </para>

    <para>
      <option>--enable-generate-docs</option>: If documentation is to be rebuilt,
add that option. Several dependencies must be installed for generating the
documentation.
    </para>

    <para>
      <command>make -j1 install</command>: ISC does not recommend any form of
parallel or job server options when doing the install.
    </para>

  </sect2>

  <sect2 role="configuration">
    <title>Configurando o Servidor DHCP Kea do ISC</title>

    <para>
      The support of IPv4, IPv6 and DDNS has been split into separate servers
which runs independently from each other. Each of them has its own
configuration file.<phrase revision="sysv"> Additional configuration files
come from the keactrl agent which is used to control the servers in an easy
way.</phrase>
    </para>

    <para>
      Consult the <ulink url="https://kea.readthedocs.io/en/kea-&kea-arm-vers;/">
Kea Administrator Reference Manual</ulink> for detailed information about
the configuration of <application>ISC Kea</application> as it is a quite
capable system.  The configuration shown below is a bare minimum to get a
DHCP server running but it already includes configuration for DDNS (Dynamic
DNS).  That setup might be working for small networks with a few clients and
low traffic. For greater installations with thousands of clients,
<application>ISC Kea</application> can be configured to use databases
(mariadb or postgresql) to store the leases and build a cluster with
multiple nodes. It can be integrated to <ulink
url="https://www.isc.org/categories/stork/">ISC Stork</ulink> which is a
management dashboard to <application>ISC Kea</application>.
    </para>

    <para>
      Se você quiser iniciar o Servidor DHCP na inicialização, instale <phrase
revision="sysv"> o conjunto de comandos sequenciais de inicialização
<filename>/etc/rc.d/init.d/kea-dhcpd</filename> incluído</phrase> <phrase
revision="systemd">a unidade <filename>kea-dhcpd.service</filename>
incluída</phrase> no pacote <xref linkend="bootscripts" revision="sysv"/>
<xref linkend="systemd-units" revision="systemd"/>:
    </para>

<screen role="root"><userinput>make install-kea-dhcpd</userinput></screen>

    <sect3 id="kea-dhcp-config">
      <title>Arquivos de Configuração</title>

      <para>
        <phrase revision="sysv"> <filename>/etc/kea/keactrl.conf</filename>,
</phrase> <filename>/etc/kea/kea-ctrl-agent.conf</filename>,
<filename>/etc/kea/kea-dhcp4.conf</filename>,
<filename>/etc/kea/kea-dhcp6.conf</filename>, and
<filename>/etc/kea/kea-dhcp-ddns.conf</filename>
      </para>

      <indexterm zone="kea keactrl-config" revision="sysv">
        <primary sortas="e-etc-kea-keactrl.conf">/etc/kea/keactrl.conf</primary>
      </indexterm>

      <indexterm zone="kea kea-ctrl-agent-config">
        <primary sortas="e-etc-kea-kea-ctrl-agent.conf">/etc/kea/kea-ctrl-agent.conf</primary>
      </indexterm>

      <indexterm zone="kea kea-dhcp4-config">
        <primary sortas="e-etc-kea-dhcp4.conf">/etc/kea/kea-dhcp4.conf</primary>
      </indexterm>

      <indexterm zone="kea kea-dhcp6-config">
        <primary sortas="e-etc-kea-dhcp6.conf">/etc/kea/kea-dhcp6.conf</primary>
      </indexterm>

      <indexterm zone="kea kea-dhcp-ddns-config">
        <primary sortas="e-etc-kea-dhcp-ddns.conf">/etc/kea/kea-dhcp-ddns.conf</primary>
      </indexterm>
    </sect3>

    <sect3 id="keactrl-config" revision="sysv">
      <title>Configuração do Controle do Kea</title>

      <para><command>keactrl</command> é usado para controlar os servidores
independentes (IPv4, IPv6, DDNS). O arquivo de configuração dele,
<filename>/etc/kea/keactrl.conf</filename>, é instalado por padrão e inclui
muitas configurações de caminho que são definidas devido ao
<command>configure</command> ao tempo da construção. Também inclui
configurações para especificar qual dos servidores deveria ser iniciado.</para>

      <itemizedlist>
        <listitem>
          <para>Agente de Controle</para>
          <para>O Agente de Controle é um processo de segundo plano que permite a
(re)configuração do serviço DHCP do Kea via API REST. Configure
<literal>ctrl_agent=yes</literal> para iniciar o agente de controle (serviço
que fornece uma API REST); configure <literal>ctrl_agent=no</literal> caso o
agente de controle não seja necessário.</para>
        </listitem>
        <listitem>
          <para>Servidor DHCP de IPv4</para>
          <para>Esse processo de segundo plano lida com solicitações de endereços
IPv4. Configure <literal>dhcp4=yes</literal> para iniciá-lo; configure
<literal>dhcp4=no</literal> caso o serviço DHCP para IPv4 não seja desejado.</para>
        </listitem>
        <listitem>
          <para>Servidor DHCP de IPv6</para>
          <para>Esse processo de segundo plano lida com solicitações de endereços
IPv6. Configure <literal>dhcp6=yes</literal> para iniciá-lo; configure
<literal>dhcp6=no</literal> caso o serviço DHCP para IPv6 não seja desejado.</para>
        </listitem>
        <listitem>
          <para>DNS Dinâmico</para>
          <para>Esse processo de segundo plano é usado para atualizar um servidor DNS
dinamicamente quando Kea atribui um endereço IP a um dispositivo. Configure
<literal>dhcp_ddns=yes</literal> para habilitá-lo; configure
<literal>dhcp_ddns=no</literal> caso as atualizações dinâmicas de DNS não
sejam desejadas.</para>
        </listitem>
      </itemizedlist>

      <para>
        The Netconf service is not installed because required dependencies are not
covered by the current BLFS book.  .    </para>

      <para>
        With the following command, Kea will be configured to start the dhcp service
for IPv4 and the dynamic DNS update, while the control agent and the dhcp
service for IPv6 remain down. Tweak the command to match your needs on
started services and execute as the &root; user:
      </para>

<screen role="root"><userinput>sed -e "s/^dhcp4=.*/dhcp4=yes/" \
    -e "s/^dhcp6=.*/dhcp6=no/" \
    -e "s/^dhcp_ddns=.*/dhcp_ddns=yes/" \
    -e "s/^ctrl_agent=.*/ctrl_agent=no/" \
    -i /etc/kea/keactrl.conf
</userinput></screen>

    </sect3>

    <sect3 id="kea-sysd-config" revision="systemd">
      <title>Kea Configuration Using Systemd Units</title>

      <para>
        Four service units are used to start various daemons provided by Kea:
      </para>

      <itemizedlist>
        <listitem>
          <para>Agente de Controle</para>
          <para>
            The Control Agent is a daemon which allows the (re)configuration of the Kea
DHCP service via REST API.  Run <command>systemctl enable
kea-ctrl-agent</command> if this daemon is needed.
          </para>
        </listitem>
        <listitem>
          <para>Servidor DHCP de IPv4</para>
          <para>
            This daemon handles requests for IPv4 addresses.  Run <command>systemctl
enable kea-dhcp4-server</command> to have it started by systemd.
          </para>
        </listitem>
        <listitem>
          <para>Servidor DHCP de IPv6</para>
          <para>
            This daemon handles requests for IPv6 addresses.  Run <command>systemctl
enable kea-dhcp6-server</command> to have it started by systemd.
          </para>
        </listitem>
        <listitem>
          <para>DNS Dinâmico</para>
          <para>
            This daemon is used to update a DNS server dynamically when Kea assigns an
IP address to a device.  Run <command>systemctl enable
kea-ddns-server</command> to have it started by systemd.
          </para>
        </listitem>
      </itemizedlist>

      <para>O serviço Netconf não é instalado porque as dependências exigidas não são
cobertas pelo atual livro do BLFS.</para>

    </sect3>

    <sect3 id="kea-ctrl-agent-config">
      <title>Configuração do Agente de Controle</title>

      <para>
        The provided configuration could be used without changes but in BLFS,
objects like sockets are stored in <filename
class="directory">/run</filename> rather than in <filename
class="directory">/tmp</filename>.
      </para>

<screen role="nodump"><userinput>cat &gt; /etc/kea/kea-ctrl-agent.conf &lt;&lt; "EOF"
<literal>// Begin /etc/kea/kea-ctrl-agent.conf
{
  // This is a basic configuration for the Kea Control Agent.
  // RESTful interface to be available at http://127.0.0.1:8000/
  "Control-agent": {
    "http-host": "127.0.0.1",
    "http-port": 8000,
    "control-sockets": {
      "dhcp4": {
        "socket-type": "unix",
        "socket-name": "/run/kea4-ctrl-socket"
      },
      "dhcp6": {
        "socket-type": "unix",
        "socket-name": "/run/kea6-ctrl-socket"
      },
      "d2": {
        "socket-type": "unix",
        "socket-name": "/run/kea-ddns-ctrl-socket"
      }
    },

    "loggers": [
      {
        "name": "kea-ctrl-agent",
        "output_options": [
          {
            "output": "/var/log/kea-ctrl-agent.log",
            "pattern": "%D{%Y-%m-%d %H:%M:%S.%q} %-5p %m\n"
          }
        ],
        "severity": "INFO",
        "debuglevel": 0
      }
    ]
  }
}
// End /etc/kea/kea-ctrl-agent.conf</literal>
EOF</userinput></screen>

    </sect3>

    <sect3 id="kea-dhcp4-config">
      <title>Configuração do Servidor DHCP de IPv4</title>

      <para>
        A sample configuration file is created in
<filename>/etc/kea/kea-dhcp4.conf</filename>.  Adjust the file to suit your
needs or overwrite it by running the following command as the &root; user
(you'll need to edit this file anyway: at least the
<emphasis>interfaces</emphasis> field, the
<emphasis>ddns-qualifying-suffix</emphasis> field, and almost all the fields
in <emphasis>Subnet4</emphasis>:
      </para>

<screen role="nodump"><userinput>cat &gt; /etc/kea/kea-dhcp4.conf &lt;&lt; "EOF"
<literal>// Inicia /etc/kea/kea-dhcp4.conf
{
  "Dhcp4": {
    // Add names of your network interfaces to listen on.
    "interfaces-config": {
      "interfaces": [ "eth0", "eth2" ]
    },

    "control-socket": {
      "socket-type": "unix",
      "socket-name": "/run/kea4-ctrl-socket"
    },

    "lease-database": {
      "type": "memfile",
      "lfc-interval": 3600
    },

    "expired-leases-processing": {
      "reclaim-timer-wait-time": 10,
      "flush-reclaimed-timer-wait-time": 25,
      "hold-reclaimed-time": 3600,
      "max-reclaim-leases": 100,
      "max-reclaim-time": 250,
      "unwarned-reclaim-cycles": 5
    },

    "renew-timer": 900,
    "rebind-timer": 1800,
    "valid-lifetime": 3600,

    // Enable DDNS - Kea will dynamically update the DNS
    "ddns-send-updates" : true,
    "ddns-qualifying-suffix": "your.domain.tld",
    "dhcp-ddns" : {
      "enable-updates": true
    },

    "subnet4": [
      {
        "subnet": "192.168.56.0/24",
        "pools": [ { "pool": "192.168.56.16 - 192.168.56.254" } ],
        "option-data": [
          {
            "name": "domain-name",
            "data": "your.domain.tld"
          },
          {
            "name": "domain-name-servers",
            "data": "192.168.56.2, 192.168.3.7"
          },
          {
            "name": "domain-search",
            "data": "your.domain.tld"
          },
          {
            "name": "routers",
            "data": "192.168.56.2"
          }
        ]
      }
    ],

    "loggers": [
      {
        "name": "kea-dhcp4",
        "output_options": [
          {
            "output": "/var/log/kea-dhcp4.log",
            "pattern": "%D{%Y-%m-%d %H:%M:%S.%q} %-5p %m\n"
          }
        ],
        "severity": "INFO",
        "debuglevel": 0
      }
    ]
  }
}
// Termina /etc/kea/kea-dhcp4.conf</literal>
EOF</userinput></screen>

    </sect3>

    <sect3 id="kea-dhcp6-config">
      <title>IPv6 DHCP Server Configuration</title>

      <para>
        A configuração para IPv6 é semelhante à configuração do IPv4. O arquivo de
configuração é <filename>/etc/kea/kea-dhcp6.conf</filename>.
      </para>

    </sect3>

    <sect3 id="kea-dhcp-ddns-config">
      <title>Configuração do DNS Dinâmico</title>

      <para>
        If there is a <xref linkend="bind"/> server running, <application>ISC
Kea</application> can update the DNS when it gives an IP address to a
client. A sample configuration file is created in
<filename>/etc/kea/kea-dhcp-ddns.conf</filename>.  Adjust the file to suit
your needs or overwrite it by running the following command as the &root;
user:
      </para>

<screen role="nodump" ><userinput>cat &gt; /etc/kea/kea-dhcp-ddns.conf &lt;&lt; "EOF"
<literal>// Inicia /etc/kea/kea-dhcp-ddns.conf
{
  "DhcpDdns": {
    "ip-address": "127.0.0.1",
    "port": 53001,
    "control-socket": {
      "socket-type": "unix",
      "socket-name": "/run/kea-ddns-ctrl-socket"
    },

    "tsig-keys": [
      {
        "name"      : "rndc-key",
        "algorithm" : "hmac-sha256",
        "secret"    : "1FU5hD7faYaajQCjSdA54JkTPQxbbPrRnzOKqHcD9cM="
      }
    ],

    "forward-ddns" : {
      "ddns-domains" : [
        {
          "name" : "your.domain.tld.",
          "key-name": "rndc-key",
          "dns-servers" : [
            {
              "ip-address" : "127.0.0.1",
              "port" : 53
            }
          ]
        }
      ]
    },

    "reverse-ddns" : {
      "ddns-domains" : [
        {
          "name" : "56.168.192.in-addr.arpa.",
          "key-name": "rndc-key",
          "dns-servers" : [
            {
              "ip-address" : "127.0.0.1",
              "port" : 53
            }
          ]
        }
      ]
    },

    "loggers": [
      {
        "name": "kea-dhcp-ddns",
        "output_options": [
          {
            "output": "/var/log/kea-ddns.log",
            "pattern": "%D{%Y-%m-%d %H:%M:%S.%q} %-5p %m\n"
          }
        ],
        "severity": "INFO",
        "debuglevel": 0
      }
    ]
  }
}
// Termina /etc/kea/kea-dhcp-ddns.conf</literal>
EOF</userinput></screen>

      <note>
        <para>
          O valor de <literal>secret</literal> é apenas um exemplo. Gere a chave para
tua instalação usando o comando <command>rndc-confgen -a</command> ou o
comando <command>tsig-keygen</command>, ambos fornecidos por <xref
linkend="bind"/>.
        </para>
        <para>
          In this example configuration, it is assumed that the DNS server runs on the
same machine as Kea does (accessible via <literal>127.0.0.1</literal>) and
that this machine has the IP <literal>192.168.56.2</literal>.
        </para>
      </note>

    </sect3>

  </sect2>

  <sect2 role="content">
    <title>Conteúdo</title>

    <segmentedlist>
      <segtitle>Aplicativos Instalados</segtitle>
      <segtitle>Bibliotecas Instaladas</segtitle>
      <segtitle>Diretórios Instalados</segtitle>

      <seglistitem>
        <seg>
          keactrl, kea-admin, kea-ctrl-agent, kea-dhcp4, kea-dhcp6, kea-dhcp-ddns,
kea-lfc, kea-shell
        </seg>
        <seg>
          libkea-asiodns.so, libkea-asiolink.so, libkea-cc.so, libkea-cgfclient.so,
libkea-cryptolink.so, libkea-d2srv.so, libkea-database.so,
libkea-dhcp_ddns.so, libkea-dhcp++.so, libkea-dhcpsrv.so, libkea-dns++.so,
libkea-eval.so, libkea-exceptions.so, libkea-hooks.so, libkea-http.so,
libkea-log.so, libkea-process.so, libkea-stats.so, libkea-tcp.so,
libkea-util.so, and libkea-util-io.so
        </seg>
        <seg>
          /etc/kea, /usr/include/kea, /usr/lib/kea,
/usr/lib/python&python3-majorver;/site-packages/kea, /usr/share/kea,
/usr/share/doc/kea-&kea-dhcp-version;, and /var/lib/kea
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descrições Curtas</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="keactrl">
        <term><command>keactrl</command></term>
        <listitem>
          <para>
            Ferramenta para controlar (iniciar/parar) os processos do servidor.
          </para>
          <indexterm zone="kea keactrl">
            <primary sortas="b-keactrl">keactrl</primary>
          </indexterm>
        </listitem>
      </varlistentry>
      <varlistentry id="kea-admin">
        <term><command>kea-admin</command></term>
        <listitem>
          <para>
            kea-admin é um conjunto de comandos sequenciais de shell que oferece
manutenção de base de dados.
          </para>
          <indexterm zone="kea kea-admin">
            <primary sortas="b-kea-admin">kea-admin</primary>
          </indexterm>
        </listitem>
      </varlistentry>
      <varlistentry id="kea-ctrl-agent">
        <term><command>kea-ctrl-agent</command></term>
        <listitem>
          <para>
            Processo de segundo plano que expõe uma interface de controle RESTful para
gerenciar servidores Kea.
          </para>
          <indexterm zone="kea kea-ctrl-agent">
            <primary sortas="b-kea-ctrl-agent">kea-ctrl-agent</primary>
          </indexterm>
        </listitem>
      </varlistentry>
      <varlistentry id="kea-dhcp4">
        <term><command>kea-dhcp4</command></term>
        <listitem>
          <para>
            O processo de segundo plano do servidor que fornece endereços IPv4.
          </para>
          <indexterm zone="kea kea-dhcp4">
            <primary sortas="b-kea-dhcp4">kea-dhcp4</primary>
          </indexterm>
        </listitem>
      </varlistentry>
      <varlistentry id="kea-dhcp6">
        <term><command>kea-dhcp6</command></term>
        <listitem>
          <para>
            O processo de segundo plano do servidor que fornece endereços IPv6.
          </para>
          <indexterm zone="kea kea-dhcp6">
            <primary sortas="b-kea-dhcp6">kea-dhcp6</primary>
          </indexterm>
        </listitem>
      </varlistentry>
      <varlistentry id="kea-dhcp-ddns">
        <term><command>kea-dhcp-ddns</command></term>
        <listitem>
          <para>
            O processo de segundo plano do servidor que realiza as atualizações
dinâmicas de DNS.
          </para>
          <indexterm zone="kea kea-dhcp-ddns">
            <primary sortas="b-kea-dhcp-ddns">kea-dhcp-ddns</primary>
          </indexterm>
        </listitem>
      </varlistentry>
      <varlistentry id="kea-lfc">
        <term><command>kea-lfc</command></term>
        <listitem>
          <para>
            O processo de serviço kea-lfc remove informações redundantes dos arquivos
usados para fornecer armazenamento persistente para a estrutura de
retaguarda de base de dados memfile. Ele é executado pelo servidor DHCP do
Kea.
          </para>
          <indexterm zone="kea kea-lfc">
            <primary sortas="b-kea-lfc">kea-lfc</primary>
          </indexterm>
        </listitem>
      </varlistentry>
      <varlistentry id="keashell">
        <term><command>keashell</command></term>
        <listitem>
          <para>
            Cliente RESTful para os serviços do <application>Kea do ISC</application>.
          </para>
          <indexterm zone="kea keashell">
            <primary sortas="b-keashell">keashell</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
