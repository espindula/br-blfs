<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY gnome-shell-download-http
           "&gnome-download-http;/gnome-shell/&gnome-50;/gnome-shell-&gnome-shell-version;.tar.xz">
  <!ENTITY gnome-shell-download-ftp  "">
  <!ENTITY gnome-shell-md5sum        "13d63cd5e0bbdd2e43034b9b906c711e">
  <!ENTITY gnome-shell-size          "2,2 MB">
  <!ENTITY gnome-shell-buildsize     "61 MB (com testes)">
  <!ENTITY gnome-shell-time          "0,2 UPC (Usando paralelismo=4; com testes)">
]>

<sect1 id="gnome-shell" xreflabel="gnome-shell-&gnome-shell-version;">
  <?dbhtml filename="gnome-shell.html"?>


  <title>gnome-shell-&gnome-shell-version;</title>

  <indexterm zone="gnome-shell">
    <primary sortas="a-gnome-shell">gnome-shell</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introdução ao GNOME Shell</title>

    <para>
      O <application>GNOME Shell</application> é a interface central de usuário(a)
do ambiente de área de trabalho do <application>GNOME</application>.
    </para>

    &lfs130_checked;

    <bridgehead renderas="sect3">Informação do Pacote</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Transferência (HTTP): <ulink url="&gnome-shell-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Transferência (FTP): <ulink url="&gnome-shell-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Soma de verificação MD5 da transferência: &gnome-shell-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Tamanho da transferência: &gnome-shell-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Espaço em disco estimado exigido: &gnome-shell-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Tempo de construção estimado: &gnome-shell-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependências do GNOME Shell</bridgehead>

    <bridgehead renderas="sect4">Exigidas</bridgehead>
    <para role="required">
      <xref linkend="evolution-data-server"/>, <xref linkend="gcr4"/>, <xref
linkend="gjs"/>, <xref linkend="gnome-desktop"/>, <xref linkend="ibus"/>,
<xref linkend="mutter"/>, <xref linkend="polkit"/> e <xref
linkend="startup-notification"/>
    </para>

    <bridgehead renderas="sect4">Recomendadas</bridgehead>
    <para role="recommended">
      <xref linkend="desktop-file-utils"/>, <xref linkend="gnome-autoar"/>, <xref
linkend="gnome-bluetooth"/>, <xref linkend="gst10-plugins-base"/>, <xref
linkend="NetworkManager"/> e <xref linkend="power-profiles-daemon"/>
    </para>

    <bridgehead renderas="sect4">Opcionais</bridgehead>
    <para role="optional">
      <xref linkend="gtk-doc"/> e <ulink
url="https://github.com/scop/bash-completion">bash-completion</ulink>
    </para>

    <bridgehead renderas="sect4">Dependências Exigidas de Tempo de Execução</bridgehead>
    <para role="required">
      <xref role="runtime" linkend="adwaita-icon-theme"/>, <xref role="runtime"
linkend="dconf"/>, <phrase revision="sysv"> <xref role='runtime'
linkend="elogind"/>, </phrase> <xref role="runtime" linkend="gdm"/>, <xref
role="runtime" linkend="gnome-control-center"/>, <phrase
revision="sysv">e</phrase> <xref role="runtime" linkend="libgweather"/>
<phrase revision="systemd">, e</phrase> <phrase revision="systemd"> <xref
role='runtime' linkend="systemd"/> </phrase>
    </para>

    <bridgehead renderas="sect4">Dependências do Recommended Runtime</bridgehead>
    <para role="recommended">
      <phrase revision='sysv'><xref linkend="blocaled" role="runtime"/> e
</phrase><xref role="runtime" linkend="gnome-menus"/>
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Instalação do GNOME Shell</title>

    <para>
      Instale o <application>GNOME Shell</application> executando os seguintes
comandos:
    </para>

<screen revision="sysv"><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

meson setup --prefix=/usr       \
            --buildtype=release \
            -D systemd=false    \
            -D tests=false      \
            ..                  &amp;&amp;
ninja</userinput></screen>

<screen revision="systemd"><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

meson setup --prefix=/usr       \
            --buildtype=release \
            -D tests=false      \
            ..                  &amp;&amp;
ninja</userinput></screen>

    <para>
      Para executar os testes, certifique-se de que o
<application>Mutter</application> esteja instalado depois de executar a
suíte de testes dele e emita:
    </para>

<screen remap='test'><userinput>meson configure -D tests=true &amp;&amp;
ninja test</userinput></screen>

    <para>
      <!-- "Failed to read monitors config file
           '/home/xry111/.config/monitors.xml': Logical monitors not
           adjacent"
           The same thing happens in mutter but it's worked around with
           HOME="".  We cannot use HOME="" here or more tests will fail.
           We also cannot tell the users to remove monitors.xml because
           doing so will purge their monitor settings.  Maybe we can
           use bwrap to run the test with an empty tmpfs mounted on $HOME
           in a separate namespace, but it's too overkill IMO.
           DO NOT REMOVE unless you've tested with a "complex multi-monitor
           configuration" (with mixed scaling settings and "strange"
           positions of monitors). -->
Você precisa estar executando em uma sessão do Wayland para executar os
testes. Alguns falharão se <xref linkend="gdm"/> não estiver
instalado. <phrase revision="sysv">Vários outros testes possivelmente falhem
no ambiente Sysv.</phrase> Um teste chamado <filename>Suporte a estilo
CSS</filename> é conhecido por falhar com algumas configurações de vários
monitores.
    </para>

    <para>
      Agora, como o(a) usuário(a) <systemitem class="username">root</systemitem>:
    </para>

<screen role="root"><userinput>ninja install</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Explicações do Comando</title>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
      href="../../xincludes/meson-buildtype-release.xml"/>



    <!--
    <para revision="sysv">

      <command>patch ...</command>: This patch fixes a bug when trying to
      start an xwayland application on non systemd.
    </para>
-->
<para revision="sysv">
      <parameter>-D systemd=false</parameter>: Impede a instalação de arquivos
systemd, que não são necessários nesta versão do BLFS.
    </para>

    <para>
      <option>-D extensions_tool=false</option>: Essa chave permite construir esse
pacote sem <xref linkend='gnome-autoar'/> instalado.
    </para>

  </sect2>

  <sect2 role="content">
    <title>Conteúdo</title>

    <segmentedlist>
      <segtitle>Aplicativos Instalados</segtitle>
      <segtitle>Bibliotecas Instaladas</segtitle>
      <segtitle>Diretórios Instalados</segtitle>

      <seglistitem>
        <seg>
          gnome-extensions, gnome-extensions-app, gnome-shell,
gnome-shell-extension-tool e gnome-shell-test-tool
        </seg>
        <seg>
          Nenhum(a)
        </seg>
        <seg>
          /usr/lib/gnome-shell, /usr/share/gnome-shell,
/usr/share/gtk-doc/html/{shell,st} (opcional) e
/usr/share/xdg-desktop-portal/
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descrições Curtas</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="gnome-shell-prog">
        <term><command>gnome-shell</command></term>
        <listitem>
          <para>
            fornece as funções centrais da interface de usuário(a) para a área de
trabalho do <application>GNOME</application>
          </para>
          <indexterm zone="gnome-shell gnome-shell-prog">
            <primary sortas="b-gnome-shell">gnome-shell</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
