<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY gnome-session-download-http
           "&gnome-download-http;/gnome-session/&gnome-50;/gnome-session-&gnome-session-version;.tar.xz">
  <!ENTITY gnome-session-download-ftp  "">
  <!ENTITY gnome-session-md5sum        "0c02fd88e93b89b8cf09f80637356ca8">
  <!ENTITY gnome-session-size          "396 KB">
  <!ENTITY gnome-session-buildsize     "7,6 MB">
  <!ENTITY gnome-session-time          "menos que 0,1 UPC">
]>

<sect1 id="gnome-session" xreflabel="gnome-session-&gnome-session-version;">
  <?dbhtml filename="gnome-session.html"?>


  <title>gnome-session-&gnome-session-version;</title>

  <indexterm zone="gnome-session">
    <primary sortas="a-gnome-session">gnome-session</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introdução ao GNOME Session</title>

    <para>
      O pacote <application>GNOME Session</application> contém o gerenciador de
sessões do <application>GNOME</application>.
    </para>

    &lfs130_checked;

    <bridgehead renderas="sect3">Informação do Pacote</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Transferência (HTTP): <ulink url="&gnome-session-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Transferência (FTP): <ulink url="&gnome-session-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Soma de verificação MD5 da transferência: &gnome-session-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Tamanho da transferência: &gnome-session-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Espaço em disco estimado exigido: &gnome-session-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Tempo de construção estimado: &gnome-session-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependências do GNOME Session</bridgehead>

    <bridgehead renderas="sect4">Exigidas</bridgehead>
    <para role="required">
      <phrase revision="sysv"><xref linkend="elogind"/>,</phrase> <xref
linkend="gnome-desktop"/>, <xref linkend="json-glib"/>, <xref
linkend="mesa"/> <phrase revision="systemd">, <xref role="runtime"
linkend="systemd"/> (tempo de execução)</phrase> e <xref linkend="upower"/>
    </para>

    <bridgehead renderas="sect4">Opcionais</bridgehead>
    <para role="optional">
      <xref linkend="xmlto"/> e <xref linkend="libxslt"/> com <xref
linkend="DocBook"/> e <xref linkend="docbook-xsl"/> (para construir a
documentação)
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Instalação do GNOME Session</title>




    <!-- in gnome-session-49, this file is no longer present as gnome-session has
     been rewritten in C.
    <para>

      When running GNOME under <xref linkend="wayland"/>, environment settings
      are not imported for the user using the system profile. The Wayland
      developers are currently undecided on a standard method to provide system
      environment settings for user sessions. To work around this limitation,
      execute the following command to make
      <command>gnome-session</command> use a login shell:
    </para>

<screen><userinput>sed 's@/bin/sh@/bin/sh -l@' -i gnome-session/gnome-session.in</userinput></screen>
-->
<!--
    <para revision="sysv">

      Adapt meson.build so that gnome-session can build without systemd:
    </para>

<screen revision="sysv"><userinput remap="pre">sed -i "/  systemd_dep/,+3d;/if enable_systemd/a \    systemd_userunitdir = '/tmp\'" meson.build</userinput></screen>
-->
<para>
      Instale o <application>GNOME Session</application> executando os seguintes
comandos:
    </para>

<screen revision="sysv"><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

meson setup --prefix=/usr              \
            --buildtype=release        \
            -D man=false               \
            -D docbook=false           \
            -D systemduserunitdir=/tmp \
            ..                         &amp;&amp;
ninja</userinput></screen>

<screen revision="systemd"><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

meson setup --prefix=/usr              \
            --buildtype=release        \
            -D man=false               \
            -D docbook=false           \
            ..                         &amp;&amp;
ninja</userinput></screen>

    <para>
      Esse pacote não vem com uma suíte de teste.
    </para>

    <para>
      Agora, como o(a) usante <systemitem class="username">root</systemitem>:
    </para>

<screen role="root"><userinput>ninja install</userinput></screen>

    <para>
      Se você gerou a documentação, mova-a para um diretório versionado:
    </para>

<screen remap="doc" role="root"><userinput>mv -v /usr/share/doc/gnome-session{,-&gnome-session-version;}</userinput></screen>


    <para revision="sysv">
      Agora, como o(a) usuário(a) <systemitem class="username">root</systemitem>,
remova as unidades systemd que são inúteis em sistemas SysV:
    </para>

<!-- this part is not needed on systemd, since the dbus session is created
by systemd-user.-->
<screen role="root" revision="sysv"><userinput>rm -rv /tmp/{*.d,*.target,*.service}</userinput></screen>


    <para revision="sysv">
      A sequência de iniciação do gnome-wayland precisa criar uma sessão dbus. No
SysV, isso tem de ser adicionado ao arquivo
<filename>gnome-wayland.desktop</filename> enviado, que é escrito para
systemd. Como o(a) usuário(a) &root;:
    </para>

<screen role="root" revision="sysv"><userinput>sed -e 's@^Exec=@&amp;/usr/bin/dbus-run-session @' \
    -i /usr/share/wayland-sessions/gnome-wayland.desktop</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Explicações do Comando</title>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
      href="../../xincludes/meson-buildtype-release.xml"/>

    <para>
      <parameter>-D man=false -D docbook=false</parameter>: Esses parâmetros
desabilitam construir documentação. Omita-os se você tiver construído as
dependências opcionais.
    </para>



    <!-- This option was removed in 46
    <para revision="sysv">

      <parameter>-D systemd_journal=false</parameter>
      and <parameter>-D systemduserunitdir=/tmp</parameter>: Allow building
      with elogind support on a SysV based system.
    </para>
-->
<para>
      <option>-D x11=true</option>: Essa opção habilita suporte para sessões do
X11. O suporte X11 está obsoleto e será removido no GNOME 50.
    </para>

  </sect2>

  <sect2 role="starting" revision="systemd">
    <title>Iniciando o GNOME</title>

    <para>
      A maneira mais fácil de iniciar o GNOME é a de usar um gerenciador de
exibição. <xref linkend="gdm"/> é o gerenciador de exibição recomendado.
    </para>

    <para>
      <!-- Bruce discovered the above regarding gdm/gnome-session. The GDM page
      now covers using Wayland on systems with NVIDIA cards as well, so I've
      commented the text below.-->
<!--One reason to start from the command line is if you want Wayland
      support on a system that has the proprietary NVIDIA driver installed,
      since GDM will not show the Wayland session type on such a system.-->
Também é possível iniciar o <application>GNOME</application> a partir da
linha de comando. No entanto, você ainda precisa ter o <xref linkend="gdm"/>
instalado para que a área de trabalho funcione. Em lançamentos anteriores do
GNOME, o gnome-shell iniciava sem ele instalado, mas isso mudou na 48.0. O
GDM não precisa estar habilitado para iniciar o gnome-shell a partir da
linha de comando.
      
      
    </para>
    
    <!-- we don't build with x11 support. So there is no point in starting
         with X11
    <warning>

      <para>
        X11 support is deprecated and will be removed in GNOME 50. Please use
        the Wayland session wherever possible.
      </para>
    </warning>

    <para>
      To start <application>GNOME</application> using
      <xref role="nodep" linkend="xinit"/>, run the following commands:
    </para>

<screen role="nodump"><userinput>cat &gt; ~/.xinitrc &lt;&lt; "EOF"
<literal>dbus-run-session gnome-session</literal>
EOF

startx</userinput></screen>
-->
<para>
      Para iniciar o <application>GNOME</application> sem um gerenciador de
exibição você precisa determinar teu ID de sessão, usando
<command>loginctl</command>. Feito isso, emita (nós supomos que o ID de
sessão seja 1):
    </para>

    <screen role="nodump"><userinput>systemctl --user set-environment XDG_SESSION_TYPE=wayland XDG_SESSION_ID=<replaceable>1</replaceable> &amp;&amp;
systemctl --user start gnome-session@gnome.target</userinput></screen>

  </sect2>

  <sect2 role="content">
    <title>Conteúdo</title>

    <segmentedlist>
      <segtitle>Aplicativos Instalados</segtitle>
      <segtitle>Bibliotecas Instaladas</segtitle>
      <segtitle>Diretórios Instalados</segtitle>

      <seglistitem>
        <seg>
          gnome-session, gnome-session-inhibit e gnome-session-quit
        </seg>
       <seg>
          Nenhum(a)
        </seg>
        <seg>
          /usr/share/doc/gnome-session-&gnome-session-version; e
/usr/share/gnome-session
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descrições Curtas</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="gnome-session-prog">
        <term><command>gnome-session</command></term>
        <listitem>
          <para>
            é usado para iniciar o ambiente de área de trabalho do
<application>GNOME</application>
          </para>
          <indexterm zone="gnome-session gnome-session-prog">
            <primary sortas="b-gnome-session">gnome-session</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="gnome-session-inhibit">
        <term><command>gnome-session-inhibit</command></term>
        <listitem>
          <para>
            é usado para inibir certas funcionalidades da sessão do
<application>GNOME</application> enquanto executar o comando dado
          </para>
          <indexterm zone="gnome-session gnome-session-inhibit">
            <primary sortas="b-gnome-session-inhibit">gnome-session-inhibit</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="gnome-session-quit">
        <term><command>gnome-session-quit</command></term>
        <listitem>
          <para>
            é usado para terminar a sessão do <application>GNOME</application>
          </para>
          <indexterm zone="gnome-session gnome-session-quit">
            <primary sortas="b-gnome-session-quit">gnome-session-quit</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
