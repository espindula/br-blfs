<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY mutter-download-http "&gnome-download-http;/mutter/&gnome-50;/mutter-&mutter-version;.tar.xz">
  <!ENTITY mutter-download-ftp  "">
  <!ENTITY mutter-md5sum        "16e91f37bb05f67c684f3582380f86a1">
  <!ENTITY mutter-size          "8,1 MB">
  <!ENTITY mutter-buildsize     "91 MB (adicionar 84 MB para os testes)">
  <!ENTITY mutter-time          "0,4 UPC (Usando paralelismo=4; adicionar 6,2 UPC para testes)">
]>

<sect1 id="mutter" xreflabel="Mutter-&mutter-version;">
  <?dbhtml filename="mutter.html"?>


  <title>Mutter-&mutter-version;</title>

  <indexterm zone="mutter">
    <primary sortas="a-Mutter">Mutter</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introdução ao Mutter</title>

    <para>
      <application>Mutter</application> é o gerenciador de janelas para o
<application>GNOME</application>. Ele não é invocado diretamente, mas a
partir da <application>Sessão GNOME</application> (em uma máquina com um
controlador de vídeo acelerado por hardware).
    </para>

    &lfs131_checked;

    <bridgehead renderas="sect3">Informação do Pacote</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Transferência (HTTP): <ulink url="&mutter-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Transferência (FTP): <ulink url="&mutter-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Soma de verificação MD5 da transferência: &mutter-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Tamanho da transferência: &mutter-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Espaço em disco estimado exigido: &mutter-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Tempo de construção estimado: &mutter-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependências do Mutter</bridgehead>

    <bridgehead renderas="sect4">Exigidas</bridgehead>
    <para role="required">
      <xref linkend="at-spi2-core"/>, <xref linkend="docutils"/>, <xref
linkend="gnome-settings-daemon"/>, <xref linkend="graphene"/>, <xref
linkend="gtk4"/>, <xref linkend="libei"/>, <xref linkend="libxcvt"/>, <xref
linkend="libxkbcommon"/> e <xref linkend="pipewire"/>
    </para>

    <bridgehead renderas="sect4">Recomendadas</bridgehead>
    <para role="recommended">
      <xref linkend="desktop-file-utils"/>, &gobject-introspection;, <xref
linkend="libdisplay-info"/> e <xref linkend="startup-notification"/>
    </para>

    <bridgehead renderas="sect4">Recomendadas (exigidas para construir o compositor Wayland)</bridgehead>
    <para role="recommended">
      <xref linkend="libinput"/>, <xref linkend="wayland"/>, <xref
linkend="wayland-protocols"/> e <xref linkend="xwayland"/>
    </para>

    <bridgehead renderas="sect4"
                revision="sysv">Recomendadas (tempo de execução)</bridgehead>
    <para role="recommended" revision="sysv">
      <xref linkend="blocaled" role="runtime"/>
    </para>

    <bridgehead renderas="sect4">Opcionais</bridgehead>
    <para role="optional">
      
      <!-- sorted as "d" instead of "p" -->
<!--<xref linkend="sysprof"/>
,-->
<xref linkend="python-dbusmock"/> e <xref linkend='gtk3'/> (ambos exigidos
para testes), <xref linkend="libadwaita1"/>, <xref linkend="xorg-server"/>
(para sessões do X11, obsoleto), <ulink
url="https://github.com/scop/bash-completion/">bash-completion</ulink>,
<ulink url="&sysprof-url;">sysprof</ulink>, <command>Xvfb</command>
(originário de <xref linkend='xorg-server'/> ou de <xref
linkend='xwayland'/>), <ulink
url="&sources-anduin-http;/mutter/xvfb-run">xvfb-run</ulink> e <ulink
url='https://gitlab.gnome.org/GNOME/zenity'>zenity</ulink> (os testes do X11
executariam somente com todos os três últimos)
    </para>

    

  <!-- NOTE: Mutter compiles shipped Cogl and Clutter for it's own
    internal use. This is because upstream have decided that Cogl and
    Clutter should be only used by Mutter in the future, so the development
    of Cogl and Clutter is now performed in the Mutter repository.  The
    "standalone" Cogl and Clutter are considered obsolete now and we have
    archived them.  -->
</sect2>

  <sect2 role="installation">
    <title>Instalação do Mutter</title>

    <para>
      Corrija um problema com os testes que, de outra forma, exigiriam que o
pacote fosse construído com informações de depuração:
    </para>

<!-- https://gitlab.gnome.org/GNOME/mutter/-/merge_requests/5102: merged in 50.3
    <para>

      Fix a private header that conflicts with pango-1.58 and later:
    </para>

<screen><userinput remap="pre">sed "/AUTOPTR/d" -i clutter/clutter/pango/clutter-pango-private.h</userinput></screen>
-->
<screen><userinput remap="pre">sed "/tests_c_args =/s/$/ + ['-U', 'G_DISABLE_ASSERT']/" -i src/tests/meson.build &amp;&amp;
sed "/c_args:/a '-U', 'G_DISABLE_ASSERT'," -i src/tests/cogl/unit/meson.build</userinput></screen>


    <para>
      Instale o <application>Mutter</application> executando os seguintes
comandos:
    </para>

<screen><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

meson setup --prefix=/usr            \
            --buildtype=release      \
            -D tests=disabled        \
            -D profiler=false        \
            -D bash_completion=false \
            ..                       &amp;&amp;
ninja</userinput></screen>

    <para>
      Se você deseja executar a suíte de teste, certifique-se de que <xref
linkend='python-dbusmock'/> e <xref linkend='gtk3'/> estejam instalados e
emita:
    </para>

<screen remap='test'><userinput>meson configure -D tests=enabled &amp;&amp;
ninja test</userinput></screen>

    <para>
      Os testes exigem uma sessão ativa do Wayland. Alguns testes são instáveis
(especialmente se teu sistema estiver sob uma carga alta), de forma que se
um teste falhar, você pode tentar executar um teste específico novamente
usando o comando <command>meson test <replaceable>&lt;nome do
teste&gt;</replaceable></command>. Alguns testes possivelmente falhem
dependendo de alguma configuração do sistema. Não faça nenhuma entrada de
mouse ou de teclado enquanto a suíte de teste estiver executando ou alguns
testes possivelmente falhem. Você também consegue testar funções básicas do
<application>Mutter</application> seguindo <xref linkend='mutter-starting'/>
depois de instalar esse pacote.
    </para>

    <para>
      Agora, como o(a) usante <systemitem class="username">root</systemitem>:
    </para>

<screen role="root"><userinput>ninja install</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Explicações do Comando</title>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
      href="../../xincludes/meson-buildtype-release.xml"/>

    <para>
      <parameter>-D bash_completion=false</parameter>: Permite construir esse
pacote sem o pacote <ulink
url="https://github.com/scop/bash-completion/">bash-completion</ulink>
instalado.
    </para>

    <para>
      <parameter>-D tests=disabled</parameter>: Permite construir esse pacote sem
<xref linkend='python-dbusmock'/> instalado. Ela será substituída por
<command>meson configure</command> se executar a suíte de teste.
    </para>

    <para>
      <parameter>-D profiler=false</parameter>: Permite construir esse pacote sem
<application>Sysprof</application>. Remova essa opção se você não tiver
instalado o <application>Sysprof</application> e desejar analisar o
desempenho de renderização do <application>Mutter</application>.
    </para>

    <para>
      <option>-D x11=true</option>: Permite construir esse pacote com suporte a
X11. O suporte a X11 está obsoleto e será removido no GNOME 50.
    </para>

  </sect2>

  <sect2 role="starting" id="mutter-starting">
    <title>Iniciando o Mutter</title>

    

    <!-- Mutter as a Wayland compositor works with recent mutter releases.
         IIRC mutter can also be used as a X window manager: just replace
         "twm" with "mutter &dash;&dash;x11" in xinitrc. But I've not tested
         such a configuration recently (I don't build Xorg Server or xinit
         these days.  -->
<para>
      <application>Mutter</application> normalmente é usado como um componente do
<application>gnome-shell</application>, mas também pode ser usado como um
compositor autônomo Wayland. Para executar <application>Mutter</application>
como um compositor Wayland, em um console virtual, emita:
    </para>

    <screen role='nodump'><userinput>mutter --wayland -- vte-2.91</userinput></screen>

    <para>
      Substitua <command>vte-2.91</command> pela linha de comando do primeiro
aplicativo que você deseja na sessão do Wayland. Observe que assim que esse
aplicativo sair, a sessão do Wayland será terminada.
    </para>

    <para>
      <application>Mutter</application> também pode funcionar como um compositor
aninhado em outra sessão do Wayland. Em um emulador de terminal, emita:
    </para>

    <screen role='nodump'><userinput>MUTTER_DEBUG_DUMMY_MODE_SPECS=1920x1080 \
dbus-run-session mutter --wayland --devkit -- vte-2.91</userinput></screen>

    <para>
      Existirá um atraso de 20 segundos antes do compositor aninhado iniciar
devido <ulink url='https://gitlab.gnome.org/GNOME/mutter/-/issues/4159'>a
defeito conhecido</ulink>. Substitua <replaceable>1920x1080</replaceable>
pelo tamanho que você quiser para a sessão aninhada Wayland.
    </para>
  </sect2>

  <sect2 role="content">
    <title>Conteúdo</title>

    <segmentedlist>
      <segtitle>Aplicativos Instalados</segtitle>
      <segtitle>Bibliotecas Instaladas</segtitle>
      <segtitle>Diretórios Instalados</segtitle>

      <seglistitem>
        <seg>
          gdctl, gnome-service-client e mutter
        </seg>
        <seg>
          libmutter-18.so e libmutter-test-18.so (opcional)
        </seg>
        <seg>
          /usr/{lib,include,libexec/installed-tests,share/{,installed-tests}}/mutter-18
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descrições Curtas</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="gdctl">
        <term><command>gdctl</command></term>
        <listitem>
          <para>
            controla a configuração atual do monitor em sessões que usam Mutter como um
gerenciador de janelas
          </para>
          <indexterm zone="mutter gdctl">
            <primary sortas="b-gdctl">gdctl</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="gnome-service-client">
        <term><command>gnome-service-client</command></term>
        <listitem>
          <para>
            inicia um cliente Wayland com uma etiqueta opcional
          </para>
          <indexterm zone="mutter gnome-service-client">
            <primary sortas="b-gnome-service-client">gnome-service-client</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="mutter-prog">
        <term><command>mutter</command></term>
        <listitem>
          <para>
            é um gerenciador de janelas de composição <application>GTK+</application>
baseado em <application>Clutter</application>
          </para>
          <indexterm zone="mutter mutter-prog">
            <primary sortas="b-mutter">mutter</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libmutter">
        <term><filename class="libraryfile">libmutter-16.so</filename></term>
        <listitem>
          <para>
            contém as funções da API do <application>Mutter</application>
          </para>
          <indexterm zone="mutter libmutter">
            <primary sortas="c-libmutter">libmutter-16.so</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libmutter-test">
        <term><filename class="libraryfile">libmutter-test-16.so</filename></term>
        <listitem>
          <para>
            contém as funções de API da suíte de teste do
<application>Mutter</application>; essa biblioteca só é instalada se a suíte
de teste desse pacote for executada e for necessária para a suíte de teste
do <application>gnome-shell</application>
          </para>
          <indexterm zone="mutter libmutter-test">
            <primary sortas="c-libmutter-test">libmutter-test-16.so</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
