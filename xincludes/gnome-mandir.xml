<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE note PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>


    
    <!-- FIXME:ken - any use is probably no longer required, everything so far
    that I've looked at actually goes into prefix/share/man - but I haven't built
    them all, so far -->
<para><parameter>--mandir=<envar>$GNOME_PREFIX</envar>/share/man</parameter>: Esse
parâmetro faz com que os arquivos <filename>man.(X)</filename> sejam
instalados em <filename
class="directory">$GNOME_PREFIX/share/man/man(X)</filename> em vez de
<filename class="directory">$GNOME_PREFIX/man/man(X)</filename>.</para>
