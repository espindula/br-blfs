<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect3 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
  ]>

  <sect3 role="commands">
    <title>Explicações do Comando</title>

    <para>
      <parameter>-w dist</parameter>: constrói o apropriado "wheel" para esse
módulo no diretório <filename class="directory">dist</filename>.
    </para>

    <para>
      <parameter>--no-build-isolation</parameter>: diz ao pip3 para executar a
construção no ambiente de sistema em vez de criar um ambiente temporário de
construção.
    </para>

    <para>
      <parameter>--no-deps</parameter>: impede pip3 de construir wheels para as
dependências do projeto.
    </para>

    <para>
      <parameter>--no-index</parameter>: ignora o índice de pacote (somente
olhando para URLs do --find-links).
    </para>

    <para>
      <parameter>--find-links dist</parameter>: procura linques para
arquivamentos, como arquivos wheel (.whl), no diretório <filename
class="directory">dist</filename>.
    </para>

    <para>
      <parameter>--no-cache-dir</parameter>: Desabilita o cache para evitar um
aviso ao instalar como o(a) usuário(a) &root;.
    </para>

    <para>
      <parameter>--no-user</parameter>: Evite erroneamente executar o comando de
instalação como um(a) usuário(a) não root.
    </para>

    <para>
      <parameter>--system-site-packages</parameter>: Permitir que o módulo venv do
<application>Python3</application> acesse o diretório <filename
class="directory">/usr/lib/python&python3-majorver;/site-packages</filename>
do sistema instalado.
    </para>

    <para>
      <option>--upgrade</option>: Atualizar o pacote para a mais recente versão
disponível. Essa opção é usada com o comando de instalação se uma versão do
pacote já estiver instalada.
    </para>

    <para>
      <option>--force-reinstall</option>: Reinstale o pacote mesmo se ele estiver
atualizado. Essa opção é usada com o comando de instalação se reinstalar-se
o pacote ou reverter-se para uma versão anterior do pacote.
    </para>

    <para>
      <option>--no-deps</option>: Não instale dependências de pacote. Essa opção
possivelmente seja necessária com as opções --upgrade ou --force-reinstall.
    </para>

  </sect3>

