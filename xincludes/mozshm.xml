<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE para PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;]>


    

    <!-- to go inside a NOTE on mozilla packages which need shm
         mounted when configured or similar in chroot.
         Unfortunately, the DTD says an xinclude can only include
         one paragraph.

         We now have exactly one paragraph, git blame this line for
         details.                                             -->
<para>
      Se você estiver compilando esse pacote em chroot, você precisa assegurar que
<filename>/dev/shm</filename> esteja montado. Se você não fizer isso, a
configuração do <application>Python</application> falhará com um informe de
rastreamento reverso referenciando
<filename>/usr/lib/pythonN.N/multiprocessing/synchronize.py</filename>. Como
o(a) usuário(a) &root;, execute: <screen role="nodump"><userinput>mountpoint -q /dev/shm || mount -t tmpfs devshm /dev/shm</userinput></screen>

    </para>
