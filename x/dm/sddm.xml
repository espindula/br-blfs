<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY sddm-download-http "https://github.com/sddm/sddm/archive/v&sddm-version;/sddm-&sddm-version;.tar.gz">
  <!ENTITY sddm-download-ftp  "">
  <!ENTITY sddm-md5sum        "e32a35c282d9be3360737eefbe25b5fa">
  <!ENTITY sddm-size          "3,4 MB">
  <!ENTITY sddm-buildsize     "24 MB">
  <!ENTITY sddm-time          "0,3 UPC (Usando paralelismo=4)">
]>

<sect1 id="sddm" xreflabel="sddm-&sddm-version;">
  <?dbhtml filename="sddm.html"?>

  <title>sddm-&sddm-version;</title>

  <indexterm zone="sddm">
    <primary sortas="a-sddm">sddm</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introdução ao sddm</title>

    <para>
      O pacote <application>sddm</application> contém um gerenciador de tela leve
baseado em <application>Qt</application> e QML.
    </para>

    &lfs130_checked;

    <bridgehead renderas="sect3">Informação do Pacote</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Transferência (HTTP): <ulink url="&sddm-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Transferência (FTP): <ulink url="&sddm-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Soma de verificação MD5 da transferência: &sddm-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Tamanho da transferência: &sddm-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Espaço em disco estimado exigido: &sddm-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Tempo de construção estimado: &sddm-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dependências do SDDM</bridgehead>

    <bridgehead renderas="sect4">Exigidas</bridgehead>
    <para role="required">
      <xref linkend="cmake"/>, <xref linkend="extra-cmake-modules"/> e <xref
linkend="qt6"/>
    </para>

    <bridgehead renderas="sect4">Recomendadas</bridgehead>
    <para role="recommended">
      <xref linkend="docutils"/> (para as páginas de manual), <xref
linkend="linux-pam"/> e <xref linkend="upower"/>
    </para>


  <!--
    <para condition="html" role="usernotes">
User Notes:
      <ulink url="&blfs-wiki;/sddm"/>
    </para>
-->
</sect2>
  <sect2 role="installation">
    <title>Instalação do SDDM</title>

    <para>
      Primeiro, crie um(a) usante e um grupo dedicado(a) para assumir o controle
do processo de segundo plano do <command>sddm</command> depois de ele ser
iniciado. Emita os seguintes comandos como o(a) usante &root;:
    </para>

<!--
    <para revision="sysv">

      Next, fix the application to start <command>upowerd</command>,
      if necessary, and after login, start the session with
      <command>ck-launch-session</command>:
    </para>

<screen revision="sysv"><userinput>sed -e '/UPOWER_SERVICE)/ s:^://:' \
    -i src/daemon/PowerManager.cpp &amp;&amp;

sed -e '/\$@$/s/exec/&amp; ck-launch-session/' \
    -i data/scripts/Xsession</userinput></screen>

    <para revision="sysv">
      For sddm-0.16.0, ConsoleKit support has been added, but is broken. Remove
      it with:
    </para>
-->
<!-- This removes also logind support. Hopefully should be fixed for next
     version
<screen revision="sysv">
<userinput>sed -e '/available.*true/s/true/false/' \
    -i src/daemon/LogindDBusTypes.cpp</userinput></screen>
-->
<screen role="root"><userinput>groupadd -g 64 sddm &amp;&amp;
useradd  -c "Processo de Segundo Plano do sddm" \
         -d /var/lib/sddm \
         -u 64 -g sddm    \
         -s /bin/false sddm</userinput></screen>


    <para>
      Instale o <application>sddm</application> executando os seguintes comandos:
    </para>

<screen revision="sysv"><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

cmake -D CMAKE_INSTALL_PREFIX=/usr        \
      -D CMAKE_BUILD_TYPE=Release         \
      -D CMAKE_POLICY_VERSION_MINIMUM=3.5 \
      -D ENABLE_JOURNALD=OFF              \
      -D NO_SYSTEMD=ON                    \
      -D RUNTIME_DIR=/run/sddm            \
      -D USE_ELOGIND=ON                   \
      -D BUILD_MAN_PAGES=ON               \
      -D BUILD_WITH_QT6=ON                \
      -D DATA_INSTALL_DIR=/usr/share/sddm \
      -D DBUS_CONFIG_FILENAME=sddm_org.freedesktop.DisplayManager.conf \
      .. &amp;&amp;
make</userinput></screen>

<screen revision="systemd"><userinput>mkdir build &amp;&amp;
cd    build &amp;&amp;

cmake -D CMAKE_INSTALL_PREFIX=/usr        \
      -D CMAKE_BUILD_TYPE=Release         \
      -D CMAKE_POLICY_VERSION_MINIMUM=3.5 \
      -D RUNTIME_DIR=/run/sddm            \
      -D BUILD_MAN_PAGES=ON               \
      -D BUILD_WITH_QT6=ON                \
      -D DATA_INSTALL_DIR=/usr/share/sddm \
      -D DBUS_CONFIG_FILENAME=sddm_org.freedesktop.DisplayManager.conf \
      .. &amp;&amp;
make</userinput></screen>

    <para>
      Esse pacote não vem com uma suíte de teste.
    </para>

    <para>
      Agora, como o(a) usante <systemitem class="username">root</systemitem>:
    </para>

<screen role="root"><userinput>make install &amp;&amp;
install -v -dm755 -o sddm -g sddm /var/lib/sddm
/usr/bin/sddm --example-config > /etc/sddm.conf</userinput></screen>

  </sect2>

  <sect2 role="commands">
    <title>Explicações do Comando</title>

    <para>
      <parameter>-D CMAKE_BUILD_TYPE=Release</parameter>: Essa chave é usada para
aplicar otimizações adicionais do compilador.
    </para>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
      href="../../xincludes/cmake-policy-version.xml"/>

    <para revision="sysv">
      <parameter>-D ENABLE_JOURNALD=OFF</parameter> e <parameter>-D
NO_SYSTEMD=ON</parameter>: Essas chaves são usadas porque essa versão do
BLFS não suporta <application>systemd</application>.
    </para>

    <para>
      <parameter>-D
DBUS_CONFIG_FILENAME=sddm_org.freedesktop.DisplayManager.conf</parameter>:
Essa chave evita que o arquivo <filename>
/etc/dbus-1/system.d/org.freedesktop.DisplayManager.conf</filename> seja
sobrescrito, pois ele possivelmente seja usado por outros DMs.
    </para>

    <para>
      <parameter>-D BUILD_MAN_PAGES=ON</parameter>: Essa chave é usada para
construir e instalar páginas de manual.
    </para>

  </sect2>

  <sect2 role="configuration">
    <title>Configurando o SDDM</title>

    <sect3 id="sddm-config">
      <title>Arquivos de Configuração</title>

      <para>
        /etc/sddm.config
      </para>

      <indexterm zone="sddm sddm-config">
        <primary sortas="e-sddm.conf">/etc/sddm.conf</primary>
      </indexterm>

      <para>
        Normalmente, você quer editar esse arquivo. Por exemplo, se o Xorg estiver
instalado em /opt, use teu editor preferido como o(a) usuário(a) &root; para
substituir o valor padrão <emphasis>XauthPath</emphasis> por
<emphasis>/opt/xorg/bin/xauth</emphasis>. Ou, como o(a) usuário(a) &root;,
emita:
      </para>

<screen role="nodump"><userinput>sed -i.orig '/ServerPath/ s|usr|opt/xorg|' /etc/sddm.conf</userinput></screen>

      <para>
        Esse comando fará a substituição e criará uma cópia do arquivo original com
o nome <filename>/etc/sddm.conf.orig</filename>.
      </para>

      <para>
        A partir de agora descreveremos como modificar configurações usando sed. É
claro que você poderá usar teu editor preferido como o(a) usuário(a) &root;.
      </para>

      <para>
        Por motivos de segurança, você normalmente quer o
<emphasis>ServerArguments=-nolisten tcp</emphasis> padrão, a menos que uma
máquina remota precise acessar o servidor X local. Nesse caso, como o(a)
usuário(a) &root;, emita:
      </para>

<screen role="nodump"><userinput>sed -i 's/-nolisten tcp//' /etc/sddm.conf</userinput></screen>

      <para>
        Usuários(as) de área de trabalho (notebook) normalmente querem ligar
(desligar) a tecla Num Lock. Para isso, como &root;, emita:
      </para>

<screen role="root"><userinput>sed -i '/Numlock/s/none/on/' /etc/sddm.conf</userinput></screen>

      <para>
        para usuários(as) de área de trabalho. Para usuários(as) de Notebook,
substitua /on/ por /off/ no comando acima.
      </para>

      <para>
        Por padrão, um teclado virtual é apresentado para o(a) usuário(a). Se isso
não for desejado, execute como &root;:
      </para>

<screen role="root"><userinput>sed -i 's/qtvirtualkeyboard//' /etc/sddm.conf</userinput></screen>

   </sect3>

    <sect3 id="sddm-bootscript">
      <title>Conjunto de Comandos Sequenciais de Inicialização</title>

      <para revision="sysv">
        Instale o conjunto de comandos sequenciais de iniciação <filename
revision="sysv">/etc/rc.d/init.d/xdm</filename> a partir do pacote <xref
linkend="bootscripts" revision="sysv"/>, como o(a) usuário(a) &root;:
      </para>

      <para revision="systemd">
        Habilite a unidade do systemd pré-instalada executando o seguinte comando
como o(a) usuário(a) &root;:
      </para>

      <indexterm zone="sddm sddm-bootscript">
        <primary sortas="f-sddm">sddm</primary>
      </indexterm>

<screen role="root" revision="sysv"><userinput>make install-sddm</userinput></screen>

<screen role="root" revision="systemd"><userinput>systemctl enable sddm</userinput></screen>

    </sect3>

    <sect3>
      <title>Configuração do Linux PAM</title>

      <note>
        <para>
          O procedimento de instalação acima instalou um conjunto de arquivos de
configuração do PAM. Esses procedimentos os sobrescrevem e usam versões
compatíveis com um ambiente BLFS.
        </para>
      </note>

      <para>
        Se você tiver construído o <application>sddm</application> com suporte ao
<application>Linux PAM</application>, crie os arquivos de configuração
necessários executando os seguintes comandos como o(a) usuário(a) &root;:
      </para>

<screen role="root" revision="sysv"><userinput>cat &gt; /etc/pam.d/sddm &lt;&lt; "EOF"
<literal># Inicia /etc/pam.d/sddm

auth     requisite      pam_nologin.so
auth     required       pam_env.so

auth     required       pam_succeed_if.so uid &gt;= 1000 quiet
auth     include        system-auth

account  include        system-account
password include        system-password

session  required       pam_limits.so
session  include        system-session

# Termina /etc/pam.d/sddm</literal>
EOF

cat &gt; /etc/pam.d/sddm-autologin &lt;&lt; "EOF"
<literal># Inicia /etc/pam.d/sddm-autologin

auth     requisite      pam_nologin.so
auth     required       pam_env.so

auth     required       pam_succeed_if.so uid &gt;= 1000 quiet
auth     required       pam_permit.so

account  include        system-account

password required       pam_deny.so

session  required       pam_limits.so
session  include        system-session

# Termina /etc/pam.d/sddm-autologin</literal>
EOF

cat &gt; /etc/pam.d/sddm-greeter &lt;&lt; "EOF"
<literal># Inicia /etc/pam.d/sddm-greeter

auth     required       pam_env.so
auth     required       pam_permit.so

account  required       pam_permit.so
password required       pam_deny.so
session  required       pam_unix.so
-session optional       pam_systemd.so

# Termina /etc/pam.d/sddm-greeter</literal>
EOF</userinput></screen>

<screen role="root" revision="systemd"><userinput>cat &gt; /etc/pam.d/sddm &lt;&lt; "EOF" &amp;&amp;
<literal># Inicia /etc/pam.d/sddm

auth     requisite      pam_nologin.so
auth     required       pam_env.so

auth     required       pam_succeed_if.so uid &gt;= 1000 quiet
auth     include        system-auth

account  include        system-account
password include        system-password

session  required       pam_limits.so
session  include        system-session

# Termina /etc/pam.d/sddm</literal>
EOF

cat &gt; /etc/pam.d/sddm-autologin &lt;&lt; "EOF" &amp;&amp;
<literal># Inicia /etc/pam.d/sddm-autologin

auth     requisite      pam_nologin.so
auth     required       pam_env.so

auth     required       pam_succeed_if.so uid &gt;= 1000 quiet
auth     required       pam_permit.so

account  include        system-account

password required       pam_deny.so

session  required       pam_limits.so
session  include        system-session

# Termina /etc/pam.d/sddm-autologin</literal>
EOF

cat &gt; /etc/pam.d/sddm-greeter &lt;&lt; "EOF"
<literal># Inicia /etc/pam.d/sddm-greeter

auth     required       pam_env.so
auth     required       pam_permit.so

account  required       pam_permit.so
password required       pam_deny.so
session  required       pam_unix.so
-session optional       pam_systemd.so

# Termina /etc/pam.d/sddm-greeter</literal>
EOF</userinput></screen>

    </sect3>

    <sect3 id="sddm-init" revision="sysv">
      <title>Iniciando o sddm</title>

      <para>
        Se o conjunto de comandos sequenciais de inicialização do sddm tiver sido
instalado, inicie o sddm executando, como o(a) usuário(a) &root;:
      </para>

<screen role="nodump"><userinput>/etc/rc.d/init.d/xdm start</userinput></screen>

      <para>
        Por convenção, o X deveria ser executado no nível de execução 5,
consequentemente, o mesmo é verdadeiro para o
<application>sddm</application>. No entanto, o nível de execução padrão é
3. Mudar para o nível de execução 5, a partir de um terminal de console,
como usuário(a) <systemitem class="username">root</systemitem>, inicia o
conjunto de comandos sequenciais de inicialização do
<command>sddm</command>, trazendo a tela de boas-vindas:
      </para>

<screen role="nodump"><userinput>init 5</userinput></screen>

      <para>
        Para a finalidade de permanentemente configurar o padrão como 5, iniciando a
tela de boas-vindas do <command>sddm</command> automaticamente, modifique o
<filename>/etc/inittab</filename> como o(a) usuário(a) <systemitem
class="username">root</systemitem>:
      </para>

<screen role="root"><userinput>cp -v /etc/inittab{,-orig} &amp;&amp;
sed -i '/initdefault/ s/3/5/' /etc/inittab</userinput></screen>

    </sect3>

    <sect3 id="sddm-sessions">
      <title>Sessões Disponíveis</title>

      <para>
        O saudador oferece uma lista de sessões disponíveis, dependendo dos
gerenciadores de janelas e ambientes de área de trabalho instalados. A lista
inclui sessões que tem um arquivo <filename>.desktop</filename>
correspondente instalado sob <filename
class="directory">/usr/share/xsessions</filename> ou <filename
class="directory">/usr/share/wayland-sessions</filename>. A maioria dos
gerenciadores de janelas e ambientes de área de trabalho fornece
automaticamente esses arquivos, mas se necessário, você pode incluir um
arquivo personalizado.
      </para>

    </sect3>

    <sect3 id="sddm-themes">
      <title>Temas</title>

      <para>
        Três temas estão instalados em <filename
class="directory">/usr/share/sddm/themes</filename>: elarun, maldives e
maya. Existe também um tema padrão, que não está presente nesse
diretório. Você consegue instalar outros temas nesse diretório. Para a
finalidade de mudar o tema, você precisa editar o
<filename>/etc/sddm.conf</filename>, para mudar o tema padrão (vazio),
substituindo <literal>Current=</literal> por
<literal>Current=<replaceable>&lt;novo tema&gt;</replaceable></literal>, por
exemplo, <literal>Current=maldives</literal>.
      </para>

      <para>
        Para a finalidade de ver o tema sem sair da sessão, emita:
      </para>

<screen role="nodump"><userinput>sddm-greeter --test-mode --theme <replaceable>&lt;caminho do tema&gt;</replaceable></userinput></screen>

    </sect3>

  <!--
    <sect3 id="sddm-Issues">

      <title>Known Issues</title>

      <para>
        This application works well, but there are issues. You find the
        mainstream known issues at
        <ulink url="https://github.com/sddm/sddm/issues">Issues</ulink>.
        The BLFS development team have found some issues.
      </para>

      <note>
        <para>
          In the next couple of paragraphs, due to a problem with sddm-greeter,
          we mention how to define the keyboard used there. Notice that this is
          also the keyboard that will be used in the X session, unless there is
          a configuration in the Desktop Environment or in the Window Manager
          overriding it, afterwards.
        </para>
      </note>

      <para>
        Keyboard selection: the greeter shows a double question mark or the
        wrong keyboard. When you start to type the password or user name
        (depending on the theme, only password), the right keyboard selection
        magically appears. Optionally, a workaround is to include the keyboard
        list in <command>/usr/share/sddm/scripts/Xsetup</command> script, as the
        <systemitem class="username">root</systemitem> user:
      </para>

<screen role="root"><userinput>echo 'setxkbmap <replaceable>"&lt;your keyboard comma separated list&gt;"</replaceable>' &gt;&gt; \
     /usr/share/sddm/scripts/Xsetup</userinput></screen>

      <para>
        E.g. <command>echo 'setxkbmap "fr,gb,br,us"' &gt;&gt;
        /usr/share/sddm/scripts/Xsetup</command>. A very accurate definition
        of the keyboard(s) is possible, for example: <command>echo 'setxkbmap
        -model pc105 -layout br,us -variant abnt2,dvorak -keycodes evdev'
        &gt;&gt; /usr/share/sddm/scripts/Xsetup</command>. See man setxkbmap.
      </para>

      <para>
        Dircolors: the <filename>/etc/dircolors</filename> file is not
        honoured. Particularly, the compressed files are not displayed in red
        colour. If this happens, a workaround is to issue, as the
        <systemitem class="username">root</systemitem> user:
      </para>

<screen role="root"><userinput>echo "source /etc/profile.d/dircolors.sh" &gt;&gt; /etc/bashrc</userinput></screen>

      <para>
        It has been reported that problems may happen with this package, if
        Xorg is installed with a prefix other than <filename
        class="directory">/usr</filename>. So far, BLFS development team has
        not hit any problem, in this case.
      </para>

    </sect3>
-->
</sect2>

  <sect2 role="content">
    <title>Conteúdo</title>

    <segmentedlist>
      <segtitle>Aplicativos Instalados</segtitle>
      <segtitle>Bibliotecas Instaladas</segtitle>
      <segtitle>Diretórios Instalados</segtitle>

      <seglistitem>
        <seg>
          sddm e sddm-greeter
        </seg>
        <seg>
          Nenhum(a)
        </seg>
        <seg>
          /usr/share/sddm e /var/lib/sddm
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descrições Curtas</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="sddm-prog">
        <term><command>sddm</command></term>
        <listitem>
          <para>
            é um gerenciador de tela e login baseado em bibliotecas
<application>Qt</application>.
          </para>
          <indexterm zone="sddm sddm-prog">
            <primary sortas="b-sddm">sddm</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="sddm-greeter">
        <term><command>sddm-greeter</command></term>
        <listitem>
          <para>
            é um processo auxiliar que exibe o saudador, uma interface gráfica de
usuário(a) que realiza a autenticação de usuário(a) e inicia o gerenciador
de janelas ou ambiente de exibição selecionado.
          </para>
          <indexterm zone="sddm sddm-greeter">
            <primary sortas="b-sddm-greeter">sddm-greeter</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
